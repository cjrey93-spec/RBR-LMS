<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_ui_page">
    <sys_ui_page action="INSERT_OR_UPDATE">
        <category>general</category>
        <client_script/>
        <description>Instructor interface for creating and managing courses, lessons, assessments, staff, and grading</description>
        <direct>true</direct>
        <endpoint>x_1879300_learning_instructor_dashboard.do</endpoint>
        <html><![CDATA[<html>
<head>
  <title>Instructor Dashboard - LMS</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <g:evaluate var="jvar_uid" expression="gs.getUserID()" />
  <g:evaluate var="jvar_uname" expression="gs.getUserName()" />
  <g:evaluate var="jvar_token" expression="gs.getSession().getSessionToken()" />
  <script>window.NOW={};window.NOW.user={};window.SN_USER_ID='${jvar_uid}';window.SN_USER_NAME='${jvar_uname}';window.SN_TOKEN='${jvar_token}';</script>
  <script>(function(){if(localStorage.getItem('lms-theme')==='dark')document.documentElement.setAttribute('data-theme','dark')})()</script>
  <g:requires name="scripts/doctype/functions_bootstrap14.js" />
  <g:requires name="scripts/lib/prototype.js" />
  <g:requires name="scripts/classes/ajax/GlideURL.js" />
  <g:requires name="scripts/doctype/CustomEventManager.js" />
  <g:requires name="scripts/classes/ajax/GlideAjax.js" />
  <g:requires name="scripts/classes/GlideUser.js" />
  <g2:client_script type="user" />
  <style>
    *{margin:0;padding:0;box-sizing:border-box}
    body{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif;background:linear-gradient(135deg,#667eea 0%,#764ba2 100%);min-height:100vh}
    .wrap{max-width:1280px;margin:0 auto;padding:20px}
    .hdr{text-align:center;color:#fff;margin-bottom:24px}
    .hdr h1{font-size:2.2rem;margin-bottom:6px}
    .hdr p{opacity:.85}
    .tabs{display:flex;flex-wrap:wrap;gap:6px;background:rgba(255,255,255,.12);border-radius:12px;padding:8px;margin-bottom:24px;backdrop-filter:blur(10px)}
    .tab-btn{padding:10px 18px;border:none;background:transparent;color:#fff;border-radius:8px;cursor:pointer;font-size:.9rem;font-weight:500;transition:all .25s}
    .tab-btn:hover{background:rgba(255,255,255,.15)}
    .tab-btn.active{background:#fff;color:#667eea;font-weight:700}
    .card{background:#fff;border-radius:16px;padding:28px;box-shadow:0 10px 40px rgba(0,0,0,.1);min-height:480px}
    .section-title{font-size:1.5rem;color:#2d3748;margin-bottom:20px}
    .form-group{margin-bottom:16px}
    .form-group label{display:block;margin-bottom:4px;font-weight:600;color:#4a5568;font-size:.9rem}
    .form-group input,.form-group textarea,.form-group select{width:100%;padding:10px 12px;border:2px solid #e2e8f0;border-radius:8px;font-size:.95rem;transition:border-color .2s;font-family:inherit}
    .form-group input:focus,.form-group textarea:focus,.form-group select:focus{outline:none;border-color:#667eea}
    .form-row{display:grid;grid-template-columns:1fr 1fr;gap:16px}
    .form-row-3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:16px}
    .btn{padding:10px 20px;border:none;border-radius:8px;cursor:pointer;font-size:.9rem;font-weight:600;transition:all .2s;display:inline-flex;align-items:center;gap:6px}
    .btn-primary{background:linear-gradient(135deg,#667eea,#764ba2);color:#fff}
    .btn-primary:hover{transform:translateY(-1px);box-shadow:0 4px 12px rgba(102,126,234,.4)}
    .btn-secondary{background:#f7fafc;color:#4a5568;border:2px solid #e2e8f0}
    .btn-secondary:hover{background:#edf2f7}
    .btn-danger{background:#fff5f5;color:#c53030;border:2px solid #fed7d7}
    .btn-danger:hover{background:#fed7d7}
    .btn-sm{padding:6px 12px;font-size:.8rem}
    .grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:16px;margin-top:16px}
    .course-card{background:#f7fafc;border:2px solid #e2e8f0;border-radius:12px;padding:18px;transition:all .2s}
    .course-card:hover{transform:translateY(-2px);box-shadow:0 6px 20px rgba(0,0,0,.1);border-color:#667eea}
    .course-card.selected{border-color:#667eea;background:#ebf4ff}
    .course-title{font-size:1.05rem;font-weight:700;color:#2d3748;margin-bottom:6px}
    .course-desc{font-size:.85rem;color:#718096;margin-bottom:10px;line-height:1.4;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}
    .meta-row{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:10px}
    .badge{padding:3px 10px;border-radius:20px;font-size:.75rem;font-weight:700}
    .badge-beginner{background:#bee3f8;color:#2b6cb0}
    .badge-intermediate{background:#fbb6ce;color:#b83280}
    .badge-advanced{background:#fbd38d;color:#c05621}
    .badge-published{background:#c6f6d5;color:#22543d}
    .badge-draft{background:#fed7d7;color:#c53030}
    .badge-video{background:#e9d8fd;color:#553c9a}
    .badge-text{background:#bee3f8;color:#2b6cb0}
    .badge-quiz{background:#fbb6ce;color:#97266d}
    .badge-assignment{background:#fbd38d;color:#c05621}
    .card-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}
    .tbl{width:100%;border-collapse:collapse;margin-top:12px}
    .tbl th{background:#f7fafc;padding:10px 12px;text-align:left;font-size:.85rem;color:#4a5568;border-bottom:2px solid #e2e8f0}
    .tbl td{padding:10px 12px;border-bottom:1px solid #e2e8f0;font-size:.9rem;color:#2d3748;vertical-align:middle}
    .tbl tr:hover td{background:#f7fafc}
    .alert{padding:12px 16px;border-radius:8px;margin-bottom:16px;font-size:.9rem}
    .alert-success{background:#c6f6d5;color:#22543d}
    .alert-error{background:#fed7d7;color:#c53030}
    .hidden{display:none!important}
    .loading{text-align:center;padding:40px;color:#718096}
    .spinner{border:3px solid #e2e8f0;border-top:3px solid #667eea;border-radius:50%;width:32px;height:32px;animation:spin .8s linear infinite;margin:0 auto 12px}
    @keyframes spin{to{transform:rotate(360deg)}}
    .empty-state{text-align:center;padding:48px;color:#a0aec0}
    .empty-state .icon{font-size:3rem;margin-bottom:12px}
    .question-block{border:2px solid #e2e8f0;border-radius:10px;padding:16px;margin-bottom:16px}
    .choice-row{display:flex;align-items:center;gap:10px;margin-bottom:8px}
    .choice-row input[type=text]{flex:1}
    .sub-card{background:#f7fafc;border-radius:8px;padding:14px;margin-bottom:12px;border-left:4px solid #667eea}
    .selected-banner{background:#ebf4ff;border:2px solid #667eea;border-radius:8px;padding:12px;margin-bottom:20px;display:flex;align-items:center;justify-content:space-between}
    .pill-row{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:16px}
    .pill{padding:6px 16px;border-radius:20px;border:2px solid #e2e8f0;background:#fff;cursor:pointer;font-size:.85rem;font-weight:500;color:#4a5568}
    .pill.active{border-color:#667eea;background:#ebf4ff;color:#553c9a}
    .two-col{display:grid;grid-template-columns:1fr 1fr;gap:24px}
    .rte-toolbar{display:flex;flex-wrap:wrap;gap:3px;padding:5px 7px;background:#f7fafc;border:2px solid #e2e8f0;border-bottom:none;border-radius:8px 8px 0 0;align-items:center}
    .rte-toolbar button{padding:3px 7px;border:1px solid #e2e8f0;background:#fff;border-radius:4px;cursor:pointer;font-size:.82rem;line-height:1.5;transition:background .1s}
    .rte-toolbar button:hover{background:#ebf4ff;border-color:#667eea;color:#553c9a}
    .rte-toolbar select{padding:2px 4px;border:1px solid #e2e8f0;border-radius:4px;font-size:.82rem;background:#fff;cursor:pointer}
    .rte-sep{width:1px;background:#e2e8f0;align-self:stretch;margin:0 2px}
    .rte-color-pick{display:inline-flex;flex-direction:column;align-items:center;gap:1px;border:1px solid #e2e8f0;border-radius:4px;padding:2px 5px;background:#fff;cursor:pointer;font-size:.72rem;font-weight:700;line-height:1.2}
    .rte-color-pick input[type=color]{width:18px;height:6px;border:none;padding:0;cursor:pointer;border-radius:2px;display:block}
    .rte-editor{min-height:160px;padding:10px 12px;border:2px solid #e2e8f0;border-radius:0 0 8px 8px;font-size:.95rem;line-height:1.6;outline:none;background:#fff;white-space:normal;overflow-y:auto}
    .rte-editor:focus{border-color:#667eea}
    .rte-editor h1{font-size:1.5rem;font-weight:700;margin:10px 0 4px}
    .rte-editor h2{font-size:1.2rem;font-weight:700;margin:8px 0 4px}
    .rte-editor h3{font-size:1rem;font-weight:700;margin:6px 0 4px}
    .rte-editor p{margin:4px 0}
    .rte-editor ul,.rte-editor ol{margin:4px 0;padding-left:28px}
    .rte-editor li{margin:2px 0}
    @counter-style decimal-paren{system:extends decimal;suffix:") "}
    @counter-style lower-alpha-paren{system:extends lower-alpha;suffix:") "}
    @counter-style upper-alpha-paren{system:extends upper-alpha;suffix:") "}
    @counter-style lower-roman-paren{system:extends lower-roman;suffix:") "}
    @counter-style upper-roman-paren{system:extends upper-roman;suffix:") "}
    @counter-style list-hyphen{system:cyclic;symbols:"-";suffix:" "}
    .rte-editor pre{background:#1a1a2e;color:#e2e8f0;padding:10px 14px;border-radius:6px;font-family:'Courier New',Consolas,monospace;font-size:.88rem;white-space:pre-wrap;margin:8px 0;display:block}
    .rte-editor img{max-width:100%;border-radius:4px;margin:4px 0;display:block}
    .rte-editor table{border-collapse:collapse;margin:8px 0;width:100%}
    .rte-editor table td,.rte-editor table th{border:1px solid #cbd5e0;padding:6px 10px;min-width:60px;vertical-align:top}
    .rte-editor table th{background:#f7fafc;font-weight:700}
    .rte-toolbar button.rte-active{background:#dbeafe!important;border-color:#667eea!important;color:#3730a3!important;font-weight:700}
    .rte-ol-start{display:none;align-items:center;gap:4px;font-size:.8rem;color:#4a5568;white-space:nowrap}
    .rte-ol-start.visible{display:inline-flex}
    .rte-ol-start input{width:44px;padding:2px 5px;border:1px solid #e2e8f0;border-radius:4px;font-size:.8rem;background:#fff}
    .rte-tbl-ctx{display:none;align-items:center;gap:2px}
    .rte-tbl-ctx.visible{display:inline-flex}
    .drag-handle{cursor:grab;color:#a0aec0;font-size:1.2rem;text-align:center;user-select:none;padding:0 4px}
    .drag-handle:active{cursor:grabbing}
    .tbl tr.drag-over td{background:#ebf4ff;border-top:2px solid #667eea}
    .tbl tr.dragging{opacity:.35}
    .dm-toggle{background:transparent;border:1px solid rgba(255,255,255,.35);color:#fff;border-radius:20px;padding:5px 14px;cursor:pointer;font-size:.95rem;line-height:1;transition:background .2s;white-space:nowrap;position:absolute;top:0;right:0}
    .dm-toggle:hover{background:rgba(255,255,255,.15)}
    html[data-theme=dark] body{background:#1a1025}
    html[data-theme=dark] .card{background:#1a1d27;box-shadow:0 10px 40px rgba(0,0,0,.5)}
    html[data-theme=dark] .section-title{color:#e2e8f0}
    html[data-theme=dark] .tab-btn{color:rgba(255,255,255,.75)}
    html[data-theme=dark] .tab-btn.active{background:#1a1d27;color:#667eea}
    html[data-theme=dark] .form-group label{color:#a0aec0}
    html[data-theme=dark] .form-group input,html[data-theme=dark] .form-group textarea,html[data-theme=dark] .form-group select{background:#252837;color:#e2e8f0;border-color:#4a5568}
    html[data-theme=dark] .form-group input:focus,html[data-theme=dark] .form-group textarea:focus,html[data-theme=dark] .form-group select:focus{border-color:#667eea}
    html[data-theme=dark] .btn-secondary{background:#252837;color:#e2e8f0;border-color:#4a5568}
    html[data-theme=dark] .btn-danger{background:#3a1a1a;color:#fc8181;border-color:#742a2a}
    html[data-theme=dark] .tbl th{background:#14172a;color:#a0aec0;border-bottom-color:#4a5568}
    html[data-theme=dark] .tbl td{border-bottom-color:#2d3748;color:#e2e8f0}
    html[data-theme=dark] .tbl tr:hover td{background:#252837}
    html[data-theme=dark] .tbl tr.drag-over td{background:#1e2348;border-top-color:#667eea}
    html[data-theme=dark] .drag-handle{color:#4a5568}
    html[data-theme=dark] .rte-toolbar{background:#14172a;border-color:#4a5568}
    html[data-theme=dark] .rte-toolbar button{background:#252837;border-color:#4a5568;color:#e2e8f0}
    html[data-theme=dark] .rte-toolbar button:hover{background:#1e2348;border-color:#667eea;color:#e2e8f0}
    html[data-theme=dark] .rte-toolbar select{background:#252837;color:#e2e8f0;border-color:#4a5568}
    html[data-theme=dark] .rte-color-pick{background:#252837;border-color:#4a5568;color:#e2e8f0}
    html[data-theme=dark] .rte-sep{background:#4a5568}
    html[data-theme=dark] .rte-editor{background:#1a1d27;color:#e2e8f0;border-color:#4a5568}
    html[data-theme=dark] .rte-editor:focus{border-color:#667eea}
    html[data-theme=dark] .rte-editor h1,html[data-theme=dark] .rte-editor h2,html[data-theme=dark] .rte-editor h3{color:#f7fafc}
    html[data-theme=dark] .alert-success{background:#1a3a23;color:#68d391}
    html[data-theme=dark] .alert-error{background:#3a1a1a;color:#fc8181}
    html[data-theme=dark] #modal-overlay>div{background:#1a1d27!important}
    html[data-theme=dark] #modal-overlay h2{color:#e2e8f0!important}
    html[data-theme=dark] #rte-popout-overlay>div{background:#1a1d27!important}
    html[data-theme=dark] #rte-popout-overlay>div>div:first-child{background:#14172a!important;border-bottom-color:#2d3748!important}
    html[data-theme=dark] #rte-popout-overlay>div>div:first-child span{color:#e2e8f0!important}
    html[data-theme=dark] .rte-toolbar button.rte-active{background:#1e2a4a!important;border-color:#667eea!important;color:#93c5fd!important}
    html[data-theme=dark] .rte-editor table td,html[data-theme=dark] .rte-editor table th{border-color:#4a5568}
    html[data-theme=dark] .rte-editor table th{background:#14172a}
    html[data-theme=dark] .rte-ol-start{color:#a0aec0}
    html[data-theme=dark] .rte-ol-start input{background:#252837;color:#e2e8f0;border-color:#4a5568}
    #rte-tbl-grid-picker{position:fixed;z-index:9999;background:#fff;border:2px solid #e2e8f0;border-radius:10px;padding:10px;box-shadow:0 4px 20px rgba(0,0,0,.25)}
    html[data-theme=dark] #rte-tbl-grid-picker{background:#1a1d27;border-color:#4a5568}
  </style>
</head>
<body>
<div class="wrap">
  <div style="display:flex;justify-content:flex-end;gap:10px;margin-bottom:10px">
    <a href="x_1879300_learning_student_dashboard.do" style="color:rgba(255,255,255,.85);text-decoration:none;font-size:.82rem;padding:5px 13px;border-radius:20px;border:1px solid rgba(255,255,255,.35)">Student Dashboard</a>
    <a href="x_1879300_learning_catalog.do" style="color:rgba(255,255,255,.85);text-decoration:none;font-size:.82rem;padding:5px 13px;border-radius:20px;border:1px solid rgba(255,255,255,.35)">Catalog</a>
    <a href="x_1879300_learning_admin_dashboard.do" style="color:rgba(255,255,255,.85);text-decoration:none;font-size:.82rem;padding:5px 13px;border-radius:20px;border:1px solid rgba(255,255,255,.35)">&#9881; Admin</a>
  </div>
  <div class="hdr" style="position:relative">
    <button id="dm-toggle" class="dm-toggle" onclick="toggleDark()" title="Toggle dark mode">&#127769;</button>
    <h1>Instructor Dashboard</h1>
    <p>Create and manage courses, lessons, assessments, and students</p>
  </div>
  <nav class="tabs">
    <button class="tab-btn active" data-tab="create">+ Create Course</button>
    <button class="tab-btn" data-tab="courses">My Courses</button>
    <button class="tab-btn" data-tab="lessons">Lessons</button>
    <button class="tab-btn" data-tab="assessments">Assessments</button>
    <button class="tab-btn" data-tab="staff">Staff</button>
    <button class="tab-btn" data-tab="grading">Grade Submissions</button>
  </nav>
  <div class="card">
    <div id="msg-global"></div>

    <!-- TAB: Create Course -->
    <div id="tab-create" class="tab-pane">
      <h2 class="section-title">Create New Course</h2>
      <form id="form-course">
        <div class="form-row">
          <div class="form-group">
            <label>Course Title *</label>
            <input type="text" name="title" required="required" placeholder="e.g. Introduction to Python" />
          </div>
          <div class="form-group">
            <label>Category *</label>
            <select name="category" required="required">
              <option value="">Select...</option>
              <option value="programming">Programming</option>
              <option value="web_development">Web Development</option>
              <option value="data_science">Data Science</option>
              <option value="devops">DevOps and Infrastructure</option>
              <option value="mobile">Mobile Development</option>
              <option value="cybersecurity">Cybersecurity</option>
              <option value="cloud">Cloud Computing</option>
              <option value="other">Other</option>
            </select>
          </div>
        </div>
        <div class="form-group">
          <label>Description *</label>
          <textarea name="description" rows="3" required="required" placeholder="What will students learn in this course?"></textarea>
        </div>
        <div class="form-row">
          <div class="form-group">
            <label>Difficulty Level *</label>
            <select name="difficulty_level" required="required">
              <option value="">Select...</option>
              <option value="beginner">Beginner</option>
              <option value="intermediate">Intermediate</option>
              <option value="advanced">Advanced</option>
            </select>
          </div>
          <div class="form-group">
            <label>Estimated Duration (hours)</label>
            <input type="number" name="estimated_duration_hours" min="1" max="500" placeholder="e.g. 10" />
          </div>
        </div>
        <div class="form-row">
          <div class="form-group">
            <label style="display:flex;align-items:center;gap:8px;cursor:pointer">
              <input type="checkbox" name="published" style="width:auto" /> Publish immediately
            </label>
          </div>
          <div class="form-group">
            <label style="display:flex;align-items:center;gap:8px;cursor:pointer">
              <input type="checkbox" name="active" checked="checked" style="width:auto" /> Active
            </label>
          </div>
        </div>
        <button type="submit" class="btn btn-primary">Create Course</button>
      </form>
    </div>

    <!-- TAB: My Courses -->
    <div id="tab-courses" class="tab-pane hidden">
      <h2 class="section-title">My Courses</h2>
      <div id="courses-list"><div class="loading"><div class="spinner"></div>Loading...</div></div>
    </div>

    <!-- TAB: Lessons -->
    <div id="tab-lessons" class="tab-pane hidden">
      <h2 class="section-title">Manage Lessons</h2>
      <div id="lessons-course-banner">
        <div class="empty-state"><div class="icon">&#128218;</div><p>Select a course from "My Courses" to manage its lessons.</p></div>
      </div>
      <div id="lessons-main" class="hidden">
        <div id="lessons-selected-banner" class="selected-banner"></div>
        <div class="two-col">
          <div>
            <h3 style="color:#2d3748;margin-bottom:12px">Add Lesson</h3>
            <form id="form-lesson">
              <div class="form-group">
                <label>Title *</label>
                <input type="text" name="title" required="required" placeholder="Lesson title" />
              </div>
              <div class="form-row">
                <div class="form-group">
                  <label>Type *</label>
                  <select name="lesson_type" id="lesson-type-select" required="required">
                    <option value="text">Text / Reading</option>
                    <option value="video">Video</option>
                    <option value="quiz">Quiz</option>
                    <option value="assignment">Assignment</option>
                  </select>
                </div>
                <div class="form-group">
                  <label>Order</label>
                  <input type="number" name="order_sequence" min="1" value="1" />
                </div>
              </div>
              <div class="form-group">
                <label>Short Description</label>
                <input type="text" name="description" placeholder="Brief description" />
              </div>
              <div class="form-group" id="lesson-content-group">
                <label>Content</label>
                <div class="rte-toolbar" onmousedown="if(event.target.tagName!=='SELECT'&amp;&amp;event.target.tagName!=='INPUT')event.preventDefault()">
                  <button type="button" onclick="rteCmd('undo')" title="Undo">&#8617;</button>
                  <button type="button" onclick="rteCmd('redo')" title="Redo">&#8618;</button>
                  <span class="rte-sep"></span>
                  <select onclick="this.value=''" onchange="rteFontSize(this.value)" data-rte-select="fontSize" title="Font Size">
                    <option value="">Size</option>
                    <option value="1">Tiny</option>
                    <option value="2">Small</option>
                    <option value="3">Normal</option>
                    <option value="4">Medium</option>
                    <option value="5">Large</option>
                    <option value="6">X-Large</option>
                    <option value="7">Huge</option>
                  </select>
                  <span class="rte-sep"></span>
                  <button type="button" data-rte-cmd="bold" onclick="rteCmd('bold')" title="Bold"><b>B</b></button>
                  <button type="button" data-rte-cmd="italic" onclick="rteCmd('italic')" title="Italic"><i>I</i></button>
                  <button type="button" data-rte-cmd="underline" onclick="rteCmd('underline')" title="Underline"><u>U</u></button>
                  <button type="button" data-rte-cmd="strikeThrough" onclick="rteCmd('strikeThrough')" title="Strikethrough"><s>S</s></button>
                  <span class="rte-sep"></span>
                  <label class="rte-color-pick" title="Text Color" onmousedown="rteSaveSelection()">
                    A<input type="color" value="#000000" oninput="rteApplyColor('foreColor',this.value)" />
                  </label>
                  <label class="rte-color-pick" title="Highlight Color" onmousedown="rteSaveSelection()">
                    H<input type="color" value="#ffff00" oninput="rteApplyColor('hiliteColor',this.value)" />
                  </label>
                  <span class="rte-sep"></span>
                  <button type="button" data-rte-cmd="justifyLeft" onclick="rteCmd('justifyLeft')" title="Align Left">&#8592;</button>
                  <button type="button" data-rte-cmd="justifyCenter" onclick="rteCmd('justifyCenter')" title="Center">&#8596;</button>
                  <button type="button" data-rte-cmd="justifyRight" onclick="rteCmd('justifyRight')" title="Align Right">&#8594;</button>
                  <button type="button" data-rte-cmd="justifyFull" onclick="rteCmd('justifyFull')" title="Justify">&#8644;</button>
                  <span class="rte-sep"></span>
                  <select onclick="this.value=''" onchange="rteBlock(this.value)" data-rte-select="block" title="Text Style">
                    <option value="">Style</option>
                    <option value="h1">H1</option>
                    <option value="h2">H2</option>
                    <option value="h3">H3</option>
                    <option value="p">&#182; Normal</option>
                  </select>
                  <span class="rte-sep"></span>
                  <select onclick="this.value=''" onchange="rteUnorderedList(this.value)" title="Bullet List" data-rte-select="ulStyle">
                    <option value="">&#8226; List</option>
                    <option value="disc">&#8226; Disc</option>
                    <option value="circle">&#9675; Circle</option>
                    <option value="square">&#9632; Square</option>
                    <option value="hyphen">&#8211; Hyphen</option>
                    <option value="none">&#8212; None</option>
                  </select>
                  <select onclick="this.value=''" onchange="rteOrderedList(this.value)" title="Numbered List" data-rte-select="olStyle">
                    <option value="">1. List</option>
                    <option value="decimal">1. 2. 3.</option>
                    <option value="lower-alpha">a. b. c.</option>
                    <option value="upper-alpha">A. B. C.</option>
                    <option value="lower-roman">i. ii. iii.</option>
                    <option value="upper-roman">I. II. III.</option>
                    <option value="decimal-p">1) 2) 3)</option>
                    <option value="lower-alpha-p">a) b) c)</option>
                    <option value="upper-alpha-p">A) B) C)</option>
                    <option value="lower-roman-p">i) ii) iii)</option>
                    <option value="upper-roman-p">I) II) III)</option>
                  </select>
                  <span class="rte-sep"></span>
                  <button type="button" onclick="rteCmd('outdent')" title="Decrease Indent">&#8647;</button>
                  <button type="button" onclick="rteCmd('indent')" title="Increase Indent">&#8649;</button>
                  <span class="rte-ol-start" onmousedown="rteSaveSelection()">Start:<input type="number" min="0" value="1" onmousedown="event.stopPropagation();rteSaveSelection()" onchange="rteRestoreSelection();rteSetOlStart(this)" title="Ordered list start number" /></span>
                  <span class="rte-sep"></span>
                  <button type="button" onclick="rteInsertCode()" title="Insert Code Block">&lt;/&gt;</button>
                  <button type="button" onclick="rteInsertImage()" title="Insert Image">&#128247;</button>
                  <button type="button" onclick="rteShowTableGrid(this)" title="Insert Table">&#9707; Tbl</button>
                  <span class="rte-tbl-ctx">
                    <button type="button" onclick="rteTableAddRow('before')" title="Insert row above">&#8593;Row+</button>
                    <button type="button" onclick="rteTableAddRow('after')" title="Insert row below">&#8595;Row+</button>
                    <button type="button" onclick="rteTableAddCol('before')" title="Insert column left">&#8592;Col+</button>
                    <button type="button" onclick="rteTableAddCol('after')" title="Insert column right">&#8594;Col+</button>
                    <button type="button" onclick="rteTableDelRow()" title="Delete row">Row&#215;</button>
                    <button type="button" onclick="rteTableDelCol()" title="Delete column">Col&#215;</button>
                    <button type="button" onclick="rteTableDel()" title="Delete table">Tbl&#215;</button>
                  </span>
                  <span class="rte-sep"></span>
                  <button type="button" onclick="rtePopout()" title="Pop-out editor" style="margin-left:2px">&#9974;&#65038; Pop-out</button>
                </div>
                <div id="lesson-content-editor" contenteditable="true" class="rte-editor" onkeydown="rteKeydown(event)" onkeyup="rteUpdateToolbar()" onmouseup="rteUpdateToolbar()"></div>
                <input type="hidden" name="content" id="lesson-content-hidden" />
              </div>
              <div class="form-group hidden" id="lesson-video-group">
                <label>Video URL</label>
                <input type="url" name="video_url" placeholder="https://www.youtube.com/watch?v=..." />
              </div>
              <div style="display:flex;gap:10px">
                <button type="submit" class="btn btn-primary">Add Lesson</button>
                <button type="button" id="cancel-edit-lesson" class="btn btn-secondary hidden" onclick="cancelEditLesson()">Cancel Edit</button>
              </div>
            </form>
          </div>
          <div>
            <h3 style="color:#2d3748;margin-bottom:12px">Existing Lessons</h3>
            <div id="lessons-list"><div class="loading"><div class="spinner"></div></div></div>
          </div>
        </div>
      </div>
    </div>

    <!-- TAB: Assessments -->
    <div id="tab-assessments" class="tab-pane hidden">
      <h2 class="section-title">Assessments</h2>
      <div id="assess-course-banner">
        <div class="empty-state"><div class="icon">&#128203;</div><p>Select a course from "My Courses" to manage assessments.</p></div>
      </div>
      <div id="assess-main" class="hidden">
        <div id="assess-selected-banner" class="selected-banner"></div>
        <div class="pill-row">
          <button class="pill active" id="pill-list" onclick="showAssessPanel('list')">Assessments</button>
          <button class="pill" id="pill-create" onclick="showAssessPanel('create')">+ New Assessment</button>
          <button class="pill hidden" id="pill-questions" onclick="showAssessPanel('questions')">Questions</button>
        </div>
        <div id="assess-panel-list">
          <div id="assessments-list"><div class="loading"><div class="spinner"></div></div></div>
        </div>
        <div id="assess-panel-create" class="hidden">
          <form id="form-assessment">
            <div class="form-row">
              <div class="form-group">
                <label>Title *</label>
                <input type="text" name="title" required="required" placeholder="e.g. Module 1 Quiz" />
              </div>
              <div class="form-group">
                <label>Type</label>
                <select name="assessment_type">
                  <option value="quiz">Quiz</option>
                  <option value="exam">Exam</option>
                  <option value="practice">Practice</option>
                  <option value="assignment">Assignment</option>
                </select>
              </div>
            </div>
            <div class="form-row-3">
              <div class="form-group">
                <label>Passing Score (%)</label>
                <input type="number" name="passing_score" min="0" max="100" value="70" />
              </div>
              <div class="form-group">
                <label>Time Limit (min, 0=none)</label>
                <input type="number" name="time_limit_minutes" min="0" value="0" />
              </div>
              <div class="form-group">
                <label>Max Attempts (0=unlimited)</label>
                <input type="number" name="max_attempts" min="0" value="3" />
              </div>
            </div>
            <div class="form-group">
              <label style="display:flex;align-items:center;gap:8px;cursor:pointer">
                <input type="checkbox" name="show_results_immediately" checked="checked" style="width:auto" />
                Show results immediately after submission
              </label>
            </div>
            <button type="submit" class="btn btn-primary">Create Assessment</button>
          </form>
        </div>
        <div id="assess-panel-questions" class="hidden">
          <div id="questions-assess-banner" class="selected-banner" style="margin-bottom:16px"></div>
          <div class="two-col">
            <div>
              <h3 style="margin-bottom:12px;color:#2d3748">Add Question</h3>
              <form id="form-question">
                <div class="form-row">
                  <div class="form-group">
                    <label>Type *</label>
                    <select name="question_type" id="q-type" required="required">
                      <option value="multiple_choice">Multiple Choice</option>
                      <option value="true_false">True / False</option>
                      <option value="short_answer">Short Answer</option>
                      <option value="essay">Essay</option>
                    </select>
                  </div>
                  <div class="form-group">
                    <label>Points</label>
                    <input type="number" name="points" min="1" value="1" />
                  </div>
                </div>
                <div class="form-group">
                  <label>Question Text *</label>
                  <textarea name="question_text" rows="2" required="required" placeholder="Enter the question..."></textarea>
                </div>
                <div id="q-choices-block">
                  <label style="font-weight:600;color:#4a5568;font-size:.9rem;margin-bottom:6px;display:block">Answer Choices (select correct)</label>
                  <div id="q-choices-list">
                    <div class="choice-row"><input type="radio" name="correct_choice" value="0" /><input type="text" class="choice-text" placeholder="Choice A" /><button type="button" class="btn btn-danger btn-sm" onclick="removeChoice(this)">X</button></div>
                    <div class="choice-row"><input type="radio" name="correct_choice" value="1" /><input type="text" class="choice-text" placeholder="Choice B" /><button type="button" class="btn btn-danger btn-sm" onclick="removeChoice(this)">X</button></div>
                  </div>
                  <button type="button" class="btn btn-secondary btn-sm" style="margin-top:8px" onclick="addChoice()">+ Add Choice</button>
                </div>
                <div id="q-tf-block" class="hidden form-group">
                  <label>Correct Answer</label>
                  <select name="tf_answer">
                    <option value="true">True</option>
                    <option value="false">False</option>
                  </select>
                </div>
                <div id="q-text-block" class="hidden form-group">
                  <label>Model Answer (for reference)</label>
                  <input type="text" name="correct_answer_text" placeholder="Expected keywords or answer" />
                </div>
                <div class="form-group" style="margin-top:10px">
                  <label>Hint (optional)</label>
                  <input type="text" name="hint" placeholder="Hint shown to students" />
                </div>
                <div class="form-group">
                  <label>Order</label>
                  <input type="number" name="order_sequence" min="1" value="1" />
                </div>
                <button type="submit" class="btn btn-primary">Add Question</button>
              </form>
            </div>
            <div>
              <h3 style="margin-bottom:12px;color:#2d3748">Questions</h3>
              <div id="questions-list"><div class="loading"><div class="spinner"></div></div></div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- TAB: Staff -->
    <div id="tab-staff" class="tab-pane hidden">
      <h2 class="section-title">Course Staff</h2>
      <div id="staff-course-banner">
        <div class="empty-state"><div class="icon">&#128101;</div><p>Select a course from "My Courses" to manage its staff.</p></div>
      </div>
      <div id="staff-main" class="hidden">
        <div id="staff-selected-banner" class="selected-banner"></div>
        <div class="form-row" style="margin-bottom:24px;align-items:end">
          <form id="form-staff" style="display:contents">
            <div class="form-group">
              <label>User sys_id</label>
              <input type="text" name="user" required="required" placeholder="User sys_id" />
            </div>
            <div class="form-group">
              <label>Role</label>
              <select name="staff_role">
                <option value="ta">Teaching Assistant</option>
                <option value="co_instructor">Co-Instructor</option>
                <option value="instructor">Instructor</option>
              </select>
            </div>
          </form>
          <div style="padding-bottom:16px">
            <button class="btn btn-primary" onclick="document.getElementById('form-staff').dispatchEvent(new Event('submit',{cancelable:true}))">Add Staff</button>
          </div>
        </div>
        <div id="staff-list"><div class="loading"><div class="spinner"></div>Loading...</div></div>
      </div>
    </div>

    <!-- TAB: Grade Submissions -->
    <div id="tab-grading" class="tab-pane hidden">
      <h2 class="section-title">Grade Student Submissions</h2>
      <div class="form-row" style="margin-bottom:16px">
        <div class="form-group">
          <label>Filter by Course</label>
          <select id="grading-course-filter"><option value="">All my courses</option></select>
        </div>
        <div class="form-group">
          <label>Status</label>
          <select id="grading-status-filter">
            <option value="submitted">Submitted (pending grading)</option>
            <option value="graded">Already graded</option>
            <option value="">All</option>
          </select>
        </div>
      </div>
      <button class="btn btn-secondary" style="margin-bottom:20px" onclick="loadSubmissions()">Refresh</button>
      <div id="submissions-list"><div class="loading"><div class="spinner"></div>Loading...</div></div>
    </div>
  </div>
</div>

<!-- Edit Course Modal -->
<div id="modal-overlay" class="hidden" style="position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:1000;display:flex;align-items:center;justify-content:center" onclick="if(event.target===this)closeEditCourse()">
  <div style="background:#fff;border-radius:16px;padding:28px;max-width:580px;width:90%;max-height:90vh;overflow-y:auto;box-shadow:0 20px 60px rgba(0,0,0,.3)">
    <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:20px">
      <h2 style="font-size:1.3rem;color:#2d3748;font-weight:700">Edit Course</h2>
      <button onclick="closeEditCourse()" class="btn btn-secondary btn-sm">&#10005;</button>
    </div>
    <div id="edit-course-msg"></div>
    <form id="form-edit-course">
      <input type="hidden" name="sys_id" />
      <div class="form-row">
        <div class="form-group">
          <label>Course Title *</label>
          <input type="text" name="title" required="required" />
        </div>
        <div class="form-group">
          <label>Category *</label>
          <select name="category" required="required">
            <option value="">Select...</option>
            <option value="programming">Programming</option>
            <option value="web_development">Web Development</option>
            <option value="data_science">Data Science</option>
            <option value="devops">DevOps and Infrastructure</option>
            <option value="mobile">Mobile Development</option>
            <option value="cybersecurity">Cybersecurity</option>
            <option value="cloud">Cloud Computing</option>
            <option value="other">Other</option>
          </select>
        </div>
      </div>
      <div class="form-group">
        <label>Description *</label>
        <textarea name="description" rows="3" required="required"></textarea>
      </div>
      <div class="form-row">
        <div class="form-group">
          <label>Difficulty Level *</label>
          <select name="difficulty_level" required="required">
            <option value="">Select...</option>
            <option value="beginner">Beginner</option>
            <option value="intermediate">Intermediate</option>
            <option value="advanced">Advanced</option>
          </select>
        </div>
        <div class="form-group">
          <label>Estimated Duration (hours)</label>
          <input type="number" name="estimated_duration_hours" min="1" max="500" />
        </div>
      </div>
      <div class="form-row">
        <div class="form-group">
          <label style="display:flex;align-items:center;gap:8px;cursor:pointer">
            <input type="checkbox" name="published" style="width:auto" /> Published
          </label>
        </div>
        <div class="form-group">
          <label style="display:flex;align-items:center;gap:8px;cursor:pointer">
            <input type="checkbox" name="active" style="width:auto" /> Active
          </label>
        </div>
      </div>
      <div style="display:flex;gap:10px;margin-top:8px">
        <button type="submit" class="btn btn-primary">Save Changes</button>
        <button type="button" class="btn btn-secondary" onclick="closeEditCourse()">Cancel</button>
      </div>
    </form>
  </div>
</div>

<!-- RTE Pop-out Modal -->
<div id="rte-popout-overlay" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.6);z-index:2000;align-items:center;justify-content:center">
  <div style="background:#fff;border-radius:16px;width:92vw;height:88vh;display:flex;flex-direction:column;box-shadow:0 20px 60px rgba(0,0,0,.4);overflow:hidden">
    <div style="display:flex;align-items:center;justify-content:space-between;padding:10px 16px;border-bottom:1px solid #e2e8f0;background:#f7fafc;flex-shrink:0;border-radius:16px 16px 0 0">
      <span style="font-weight:700;color:#2d3748;font-size:1rem">Lesson Content Editor</span>
      <div style="display:flex;gap:8px">
        <button type="button" class="btn btn-primary btn-sm" onclick="rtePopoutClose(true)">&#10003; Apply &amp; Close</button>
        <button type="button" class="btn btn-secondary btn-sm" onclick="rtePopoutClose(false)">&#10005; Cancel</button>
      </div>
    </div>
    <div class="rte-toolbar" onmousedown="if(event.target.tagName!=='SELECT'&amp;&amp;event.target.tagName!=='INPUT')event.preventDefault()" style="border-radius:0;border-left:none;border-right:none;border-top:none;flex-shrink:0">
      <button type="button" onclick="rteCmd('undo')" title="Undo">&#8617;</button>
      <button type="button" onclick="rteCmd('redo')" title="Redo">&#8618;</button>
      <span class="rte-sep"></span>
      <select onclick="this.value=''" onchange="rteFontSize(this.value)" data-rte-select="fontSize" title="Font Size">
        <option value="">Size</option>
        <option value="1">Tiny</option>
        <option value="2">Small</option>
        <option value="3">Normal</option>
        <option value="4">Medium</option>
        <option value="5">Large</option>
        <option value="6">X-Large</option>
        <option value="7">Huge</option>
      </select>
      <span class="rte-sep"></span>
      <button type="button" data-rte-cmd="bold" onclick="rteCmd('bold')" title="Bold"><b>B</b></button>
      <button type="button" data-rte-cmd="italic" onclick="rteCmd('italic')" title="Italic"><i>I</i></button>
      <button type="button" data-rte-cmd="underline" onclick="rteCmd('underline')" title="Underline"><u>U</u></button>
      <button type="button" data-rte-cmd="strikeThrough" onclick="rteCmd('strikeThrough')" title="Strikethrough"><s>S</s></button>
      <span class="rte-sep"></span>
      <label class="rte-color-pick" title="Text Color" onmousedown="rteSaveSelection()">
        A<input type="color" value="#000000" oninput="rteApplyColor('foreColor',this.value)" />
      </label>
      <label class="rte-color-pick" title="Highlight Color" onmousedown="rteSaveSelection()">
        H<input type="color" value="#ffff00" oninput="rteApplyColor('hiliteColor',this.value)" />
      </label>
      <span class="rte-sep"></span>
      <button type="button" data-rte-cmd="justifyLeft" onclick="rteCmd('justifyLeft')" title="Align Left">&#8592;</button>
      <button type="button" data-rte-cmd="justifyCenter" onclick="rteCmd('justifyCenter')" title="Center">&#8596;</button>
      <button type="button" data-rte-cmd="justifyRight" onclick="rteCmd('justifyRight')" title="Align Right">&#8594;</button>
      <button type="button" data-rte-cmd="justifyFull" onclick="rteCmd('justifyFull')" title="Justify">&#8644;</button>
      <span class="rte-sep"></span>
      <select onclick="this.value=''" onchange="rteBlock(this.value)" data-rte-select="block" title="Text Style">
        <option value="">Style</option>
        <option value="h1">H1</option>
        <option value="h2">H2</option>
        <option value="h3">H3</option>
        <option value="p">&#182; Normal</option>
      </select>
      <span class="rte-sep"></span>
      <select onclick="this.value=''" onchange="rteUnorderedList(this.value)" title="Bullet List" data-rte-select="ulStyle">
        <option value="">&#8226; List</option>
        <option value="disc">&#8226; Disc</option>
        <option value="circle">&#9675; Circle</option>
        <option value="square">&#9632; Square</option>
        <option value="hyphen">&#8211; Hyphen</option>
        <option value="none">&#8212; None</option>
      </select>
      <select onclick="this.value=''" onchange="rteOrderedList(this.value)" title="Numbered List" data-rte-select="olStyle">
        <option value="">1. List</option>
        <option value="decimal">1. 2. 3.</option>
        <option value="lower-alpha">a. b. c.</option>
        <option value="upper-alpha">A. B. C.</option>
        <option value="lower-roman">i. ii. iii.</option>
        <option value="upper-roman">I. II. III.</option>
        <option value="decimal-p">1) 2) 3)</option>
        <option value="lower-alpha-p">a) b) c)</option>
        <option value="upper-alpha-p">A) B) C)</option>
        <option value="lower-roman-p">i) ii) iii)</option>
        <option value="upper-roman-p">I) II) III)</option>
      </select>
      <span class="rte-sep"></span>
      <button type="button" onclick="rteCmd('outdent')" title="Decrease Indent">&#8647;</button>
      <button type="button" onclick="rteCmd('indent')" title="Increase Indent">&#8649;</button>
      <span class="rte-ol-start" onmousedown="rteSaveSelection()">Start:<input type="number" min="0" value="1" onmousedown="event.stopPropagation();rteSaveSelection()" onchange="rteRestoreSelection();rteSetOlStart(this)" title="Ordered list start number" /></span>
      <span class="rte-sep"></span>
      <button type="button" onclick="rteInsertCode()" title="Insert Code Block">&lt;/&gt;</button>
      <button type="button" onclick="rteInsertImage()" title="Insert Image">&#128247;</button>
      <button type="button" onclick="rteShowTableGrid(this)" title="Insert Table">&#9707; Tbl</button>
      <span class="rte-tbl-ctx">
        <button type="button" onclick="rteTableAddRow('before')" title="Insert row above">&#8593;Row+</button>
        <button type="button" onclick="rteTableAddRow('after')" title="Insert row below">&#8595;Row+</button>
        <button type="button" onclick="rteTableAddCol('before')" title="Insert column left">&#8592;Col+</button>
        <button type="button" onclick="rteTableAddCol('after')" title="Insert column right">&#8594;Col+</button>
        <button type="button" onclick="rteTableDelRow()" title="Delete row">Row&#215;</button>
        <button type="button" onclick="rteTableDelCol()" title="Delete column">Col&#215;</button>
        <button type="button" onclick="rteTableDel()" title="Delete table">Tbl&#215;</button>
      </span>
    </div>
    <div id="lesson-content-editor-popup" contenteditable="true" class="rte-editor" onkeydown="rteKeydown(event)" onkeyup="rteUpdateToolbar()" onmouseup="rteUpdateToolbar()" style="flex:1;border:none;border-radius:0;min-height:0;overflow-y:auto;padding:16px 20px"></div>
  </div>
</div>

<script>
function initDark(){var d=document.documentElement.getAttribute('data-theme')==='dark';var b=document.getElementById('dm-toggle');if(b)b.textContent=d?'\u2600\uFE0F':'\uD83C\uDF19';}
function toggleDark(){var d=document.documentElement.getAttribute('data-theme')==='dark';document.documentElement.setAttribute('data-theme',d?'light':'dark');localStorage.setItem('lms-theme',d?'light':'dark');var b=document.getElementById('dm-toggle');if(b)b.textContent=d?'\uD83C\uDF19':'\u2600\uFE0F';}
initDark();
var API = '/api/now/table';
var SCOPE = 'x_1879300_learning';
var currentUser = window.SN_USER_ID || '';
var currentUserName = window.SN_USER_NAME || '';
var selectedCourse = null;
var selectedAssessment = null;
var myCourses = [];
var editingLessonId = null;
var lessonsList = [];
var dragSrcRow = null;
var activeRteId = 'lesson-content-editor';
var savedRteRange = null;

function hdrs() {
  var ck = window.SN_TOKEN || window.g_ck || (typeof g_ck !== 'undefined' ? g_ck : '');
  console.log('[LMS] hdrs g_ck:', ck ? ck.substring(0,8) + '...' : 'EMPTY');
  return { 'Accept': 'application/json', 'Content-Type': 'application/json', 'X-UserToken': ck };
}

function showMsg(msg, type) {
  var el = document.getElementById('msg-global');
  el.innerHTML = '<div class="alert alert-' + type + '">' + msg + '</div>';
  if (type !== 'error') setTimeout(function(){ el.innerHTML = ''; }, 5000);
}

function getVal(obj, field) {
  if (!obj) return '';
  var v = obj[field];
  if (v && typeof v === 'object') return v.display_value || v.value || '';
  return v || '';
}

function getRawVal(obj, field) {
  if (!obj) return '';
  var v = obj[field];
  if (v && typeof v === 'object') return v.value || '';
  return v || '';
}

function escQ(s) { return (s || '').replace(/'/g, "\\'"); }

function getRteEditor() {
  return document.getElementById(activeRteId);
}

function rteSaveSelection() {
  var sel = window.getSelection();
  if (sel && sel.rangeCount) savedRteRange = sel.getRangeAt(0).cloneRange();
}

function rteRestoreSelection() {
  if (!savedRteRange) return;
  var editor = getRteEditor();
  if (editor) editor.focus();
  var sel = window.getSelection();
  if (sel) { sel.removeAllRanges(); sel.addRange(savedRteRange); }
}

function rteCmd(cmd) {
  getRteEditor().focus();
  document.execCommand(cmd, false, null);
}

function rteBlock(tag) {
  if (!tag) return;
  getRteEditor().focus();
  document.execCommand('formatBlock', false, tag);
}

function rteFontSize(size) {
  if (!size) return;
  getRteEditor().focus();
  document.execCommand('fontSize', false, size);
}

function rteApplyColor(cmd, color) {
  rteRestoreSelection();
  document.execCommand(cmd, false, color);
}

var OL_PAREN_STYLE = {'decimal-p':'decimal-paren','lower-alpha-p':'lower-alpha-paren','upper-alpha-p':'upper-alpha-paren','lower-roman-p':'lower-roman-paren','upper-roman-p':'upper-roman-paren'};

function applyOlStyle(ol, type) {
  ol.style.listStyleType = OL_PAREN_STYLE[type] || type;
}

function applyUlStyle(ul, type) {
  ul.style.listStyleType = type === 'hyphen' ? 'list-hyphen' : type;
}

function rteKeydown(e) {
  if (e.key === 'Tab') {
    var sel = window.getSelection();
    if (sel && sel.anchorNode) {
      var n = sel.anchorNode;
      while (n) {
        if (n.tagName === 'LI') {
          e.preventDefault();
          document.execCommand(e.shiftKey ? 'outdent' : 'indent', false, null);
          return;
        }
        if (n.contentEditable === 'true') break;
        n = n.parentNode;
      }
    }
  }
}

function rteOrderedList(type) {
  if (!type) return;
  var editor = getRteEditor();
  editor.focus();
  var sel = window.getSelection();
  var existingOl = null;
  if (sel && sel.anchorNode) {
    var n = sel.anchorNode;
    while (n && n !== editor) { if (n.tagName === 'OL') { existingOl = n; break; } n = n.parentNode; }
  }
  if (existingOl) {
    applyOlStyle(existingOl, type);
  } else {
    document.execCommand('insertOrderedList', false, null);
    sel = window.getSelection();
    if (sel && sel.anchorNode) {
      var n2 = sel.anchorNode;
      while (n2 && n2 !== editor) { if (n2.tagName === 'OL') { applyOlStyle(n2, type); break; } n2 = n2.parentNode; }
    }
  }
}

function rteUnorderedList(type) {
  if (!type) return;
  var editor = getRteEditor();
  editor.focus();
  var sel = window.getSelection();
  var existingUl = null;
  if (sel && sel.anchorNode) {
    var n = sel.anchorNode;
    while (n && n !== editor) { if (n.tagName === 'UL') { existingUl = n; break; } n = n.parentNode; }
  }
  if (existingUl) {
    applyUlStyle(existingUl, type);
  } else {
    document.execCommand('insertUnorderedList', false, null);
    sel = window.getSelection();
    if (sel && sel.anchorNode) {
      var n2 = sel.anchorNode;
      while (n2 && n2 !== editor) { if (n2.tagName === 'UL') { applyUlStyle(n2, type); break; } n2 = n2.parentNode; }
    }
  }
}

function rteInsertCode() {
  var editor = getRteEditor();
  editor.focus();
  var sel = window.getSelection();
  if (!sel || !sel.rangeCount) return;
  var range = sel.getRangeAt(0);
  var text = sel.toString() || '// code here';
  var pre = document.createElement('pre');
  pre.textContent = text;
  range.deleteContents();
  range.insertNode(pre);
  var p = document.createElement('p');
  p.innerHTML = '<br>';
  if (pre.nextSibling) pre.parentNode.insertBefore(p, pre.nextSibling);
  else pre.parentNode.appendChild(p);
  range.setStartAfter(pre);
  range.collapse(true);
  sel.removeAllRanges();
  sel.addRange(range);
}

function rteInsertImage() {
  var url = prompt('Image URL:');
  if (!url || !url.trim()) return;
  var editor = getRteEditor();
  editor.focus();
  document.execCommand('insertHTML', false, '<img src="' + url.replace(/"/g, '&quot;') + '" style="max-width:100%;border-radius:4px;margin:4px 0" /><p><br></p>');
}

// ── Toolbar state sync ────────────────────────────────────────────────
function rteGetAncestor(tag) {
  var sel = window.getSelection();
  if (!sel || !sel.anchorNode) return null;
  var editor = getRteEditor();
  var n = sel.anchorNode;
  if (n.nodeType === 3) n = n.parentNode;
  while (n && n !== editor) {
    if (n.tagName === tag) return n;
    n = n.parentNode;
  }
  return null;
}

function rteUpdateToolbar() {
  // Button active states
  ['bold','italic','underline','strikeThrough','justifyLeft','justifyCenter','justifyRight','justifyFull'].forEach(function(cmd) {
    var active = false;
    try { active = !!document.queryCommandState(cmd); } catch(e) {}
    document.querySelectorAll('[data-rte-cmd="' + cmd + '"]').forEach(function(b) {
      b.classList.toggle('rte-active', active);
    });
  });
  // Block style
  var blockVal = '';
  try { blockVal = (document.queryCommandValue('formatBlock') || '').toLowerCase(); } catch(e) {}
  document.querySelectorAll('[data-rte-select="block"]').forEach(function(s) { s.value = blockVal; });
  // Font size
  var szVal = '';
  try { szVal = document.queryCommandValue('fontSize') || ''; } catch(e) {}
  document.querySelectorAll('[data-rte-select="fontSize"]').forEach(function(s) { s.value = szVal; });
  // Table context controls — visible only when cursor is inside a table cell
  var inTbl = !!(rteGetAncestor('TD') || rteGetAncestor('TH'));
  document.querySelectorAll('.rte-tbl-ctx').forEach(function(el) { el.classList.toggle('visible', inTbl); });
  // OL start control — visible only when cursor is inside an ordered list
  var ol = rteGetAncestor('OL');
  document.querySelectorAll('.rte-ol-start').forEach(function(el) {
    el.classList.toggle('visible', !!ol);
    if (ol) { var inp = el.querySelector('input'); if (inp) inp.value = ol.getAttribute('start') || '1'; }
  });
  // UL / OL list-style-type select sync
  var ul = rteGetAncestor('UL');
  var ulSelVal = '';
  if (ul) { var ust = ul.style.listStyleType || ''; ulSelVal = ust === 'list-hyphen' ? 'hyphen' : (ust || 'disc'); }
  document.querySelectorAll('[data-rte-select="ulStyle"]').forEach(function(s) { s.value = ulSelVal; });
  var OL_PAREN_REV = {'decimal-paren':'decimal-p','lower-alpha-paren':'lower-alpha-p','upper-alpha-paren':'upper-alpha-p','lower-roman-paren':'lower-roman-p','upper-roman-paren':'upper-roman-p'};
  var olSelVal = '';
  if (ol) { var olst = ol.style.listStyleType || ''; olSelVal = OL_PAREN_REV[olst] || (olst || 'decimal'); }
  document.querySelectorAll('[data-rte-select="olStyle"]').forEach(function(s) { s.value = olSelVal; });
}

// Sync toolbar state on every selection change in either editor
document.addEventListener('selectionchange', function() {
  var ae = document.activeElement;
  if (ae && ae.classList && ae.classList.contains('rte-editor')) rteUpdateToolbar();
});

// ── Table grid picker ─────────────────────────────────────────────────
function rteShowTableGrid(btn) {
  var overlay = document.getElementById('rte-tbl-grid-overlay');
  if (!overlay) {
    overlay = document.createElement('div');
    overlay.id = 'rte-tbl-grid-overlay';
    overlay.style.cssText = 'position:fixed;inset:0;z-index:9998;display:none';
    overlay.addEventListener('click', function(e) { if (e.target === overlay) rteHideTableGrid(); });
    var picker = document.createElement('div');
    picker.id = 'rte-tbl-grid-picker';
    var cells = document.createElement('div');
    cells.id = 'rte-tbl-grid-cells';
    for (var r = 1; r <= 8; r++) {
      var row = document.createElement('div');
      row.style.cssText = 'display:flex;gap:3px;margin-bottom:3px';
      for (var c = 1; c <= 8; c++) {
        (function(rr, cc) {
          var cell = document.createElement('div');
          cell.style.cssText = 'width:16px;height:16px;border:1px solid #e2e8f0;border-radius:2px;cursor:pointer;background:#f7fafc;flex-shrink:0;transition:background .08s';
          cell.addEventListener('mouseover', function() { rteTableGridHover(rr, cc); });
          cell.addEventListener('click', function(e) { e.stopPropagation(); rteHideTableGrid(); rteInsertTable(rr, cc); });
          row.appendChild(cell);
        })(r, c);
      }
      cells.appendChild(row);
    }
    var lbl = document.createElement('div');
    lbl.id = 'rte-tbl-grid-lbl';
    lbl.style.cssText = 'font-size:.75rem;text-align:center;margin-top:6px;color:#718096';
    lbl.textContent = 'Click to insert table';
    picker.appendChild(cells);
    picker.appendChild(lbl);
    overlay.appendChild(picker);
    document.body.appendChild(overlay);
  }
  rteSaveSelection();
  var rect = btn.getBoundingClientRect();
  var picker2 = document.getElementById('rte-tbl-grid-picker');
  picker2.style.top = (rect.bottom + 4) + 'px';
  picker2.style.left = rect.left + 'px';
  overlay.style.display = 'block';
  rteTableGridHover(1, 1);
}

function rteHideTableGrid() {
  var overlay = document.getElementById('rte-tbl-grid-overlay');
  if (overlay) overlay.style.display = 'none';
}

function rteTableGridHover(rows, cols) {
  var cells = document.getElementById('rte-tbl-grid-cells');
  if (!cells) return;
  Array.from(cells.children).forEach(function(row, r) {
    Array.from(row.children).forEach(function(cell, c) {
      var on = r < rows && c < cols;
      cell.style.background = on ? '#667eea' : '#f7fafc';
      cell.style.borderColor = on ? '#667eea' : '#e2e8f0';
    });
  });
  var lbl = document.getElementById('rte-tbl-grid-lbl');
  if (lbl) lbl.textContent = rows + ' \u00d7 ' + cols;
}

// ── Table insertion ───────────────────────────────────────────────────
function rteInsertTable(rows, cols) {
  rteRestoreSelection();
  var cellStyle = 'border:1px solid #cbd5e0;padding:6px 10px;min-width:60px;vertical-align:top';
  var html = '<table style="border-collapse:collapse;width:100%;margin:8px 0"><tbody>';
  for (var r = 0; r < rows; r++) {
    html += '<tr>';
    for (var c = 0; c < cols; c++) {
      var tag = r === 0 ? 'th' : 'td';
      var extra = r === 0 ? ';background:#f7fafc;font-weight:700' : '';
      html += '<' + tag + ' style="' + cellStyle + extra + '"><br></' + tag + '>';
    }
    html += '</tr>';
  }
  html += '</tbody></table><p><br></p>';
  document.execCommand('insertHTML', false, html);
}

// ── Table row / column operations ─────────────────────────────────────
function rteTableAddRow(dir) {
  var td = rteGetAncestor('TD') || rteGetAncestor('TH');
  if (!td) return;
  var row = td.parentElement;
  var colCount = row.cells.length;
  var newRow = row.cloneNode(false);
  var cs = 'border:1px solid #cbd5e0;padding:6px 10px;min-width:60px;vertical-align:top';
  for (var i = 0; i < colCount; i++) {
    var cell = document.createElement('td');
    cell.setAttribute('style', cs);
    cell.innerHTML = '<br>';
    newRow.appendChild(cell);
  }
  row.parentNode.insertBefore(newRow, dir === 'before' ? row : row.nextSibling);
}

function rteTableAddCol(dir) {
  var td = rteGetAncestor('TD') || rteGetAncestor('TH');
  if (!td) return;
  var colIdx = td.cellIndex;
  var table = td.closest ? td.closest('table') : (function(n){ while(n && n.tagName !== 'TABLE') n = n.parentNode; return n; })(td);
  if (!table) return;
  var insertIdx = dir === 'before' ? colIdx : colIdx + 1;
  var cs = 'border:1px solid #cbd5e0;padding:6px 10px;min-width:60px;vertical-align:top';
  Array.from(table.rows).forEach(function(r) {
    var isHeader = r.cells[0] && r.cells[0].tagName === 'TH';
    var newCell = document.createElement(isHeader ? 'th' : 'td');
    newCell.setAttribute('style', cs + (isHeader ? ';background:#f7fafc;font-weight:700' : ''));
    newCell.innerHTML = '<br>';
    if (insertIdx >= r.cells.length) r.appendChild(newCell);
    else r.insertBefore(newCell, r.cells[insertIdx]);
  });
}

function rteTableDelRow() {
  var td = rteGetAncestor('TD') || rteGetAncestor('TH');
  if (!td) return;
  var row = td.parentElement;
  var table = td.closest ? td.closest('table') : (function(n){ while(n && n.tagName !== 'TABLE') n = n.parentNode; return n; })(td);
  if (!table || table.rows.length <= 1) return;
  row.parentNode.removeChild(row);
}

function rteTableDelCol() {
  var td = rteGetAncestor('TD') || rteGetAncestor('TH');
  if (!td) return;
  var colIdx = td.cellIndex;
  var table = td.closest ? td.closest('table') : (function(n){ while(n && n.tagName !== 'TABLE') n = n.parentNode; return n; })(td);
  if (!table || (table.rows[0] && table.rows[0].cells.length <= 1)) return;
  Array.from(table.rows).forEach(function(r) { if (r.cells[colIdx]) r.deleteCell(colIdx); });
}

function rteTableDel() {
  var editor = getRteEditor();
  var sel = window.getSelection();
  if (!sel || !sel.anchorNode) return;
  var n = sel.anchorNode;
  if (n.nodeType === 3) n = n.parentNode;
  while (n && n !== editor) {
    if (n.tagName === 'TABLE') { n.parentNode.removeChild(n); return; }
    n = n.parentNode;
  }
}

// ── Ordered list custom start ─────────────────────────────────────────
function rteSetOlStart(input) {
  // Try current selection first, then fall back to the saved range
  var ol = rteGetAncestor('OL');
  if (!ol && savedRteRange) {
    var n = savedRteRange.startContainer;
    if (n.nodeType === 3) n = n.parentNode;
    var editor = getRteEditor();
    while (n && n !== editor) {
      if (n.tagName === 'OL') { ol = n; break; }
      n = n.parentNode;
    }
  }
  if (!ol) return;
  var val = parseInt(input.value, 10);
  if (!isNaN(val) && val >= 0) ol.setAttribute('start', val);
}

function rtePopout() {
  document.getElementById('lesson-content-editor-popup').innerHTML = document.getElementById('lesson-content-editor').innerHTML;
  activeRteId = 'lesson-content-editor-popup';
  document.getElementById('rte-popout-overlay').style.display = 'flex';
  setTimeout(function(){ document.getElementById('lesson-content-editor-popup').focus(); }, 50);
}

function rtePopoutClose(apply) {
  if (apply) {
    document.getElementById('lesson-content-editor').innerHTML = document.getElementById('lesson-content-editor-popup').innerHTML;
  }
  activeRteId = 'lesson-content-editor';
  document.getElementById('rte-popout-overlay').style.display = 'none';
}

// Tab switching
document.querySelectorAll('.tab-btn').forEach(function(btn) {
  btn.addEventListener('click', function() {
    document.querySelectorAll('.tab-btn').forEach(function(b){ b.classList.remove('active'); });
    document.querySelectorAll('.tab-pane').forEach(function(p){ p.classList.add('hidden'); });
    btn.classList.add('active');
    var tab = btn.dataset.tab;
    document.getElementById('tab-' + tab).classList.remove('hidden');
    if (tab === 'courses') loadMyCourses();
    if (tab === 'grading') loadSubmissions();
  });
});

// Create Course
document.getElementById('form-course').addEventListener('submit', function(e) {
  e.preventDefault();
  // Fallback: resolve user identity if Jelly substitution was empty
  if (!currentUser && typeof g_user !== 'undefined' && g_user.userID) {
    currentUser = g_user.userID;
    currentUserName = g_user.userName;
  }
  console.log('[LMS] createCourse as user:', currentUser, '/', currentUserName);
  var fd = new FormData(e.target);
  var body = {
    title: fd.get('title'),
    description: fd.get('description'),
    category: fd.get('category'),
    difficulty_level: fd.get('difficulty_level'),
    estimated_duration_hours: fd.get('estimated_duration_hours') || 0,
    published: fd.has('published'),
    active: fd.has('active'),
    created_by: currentUser
  };
  fetch(API + '/' + SCOPE + '_courses', { method: 'POST', headers: hdrs(), body: JSON.stringify(body) })
    .then(function(r){ if (!r.ok) throw new Error(); showMsg('Course created!', 'success'); e.target.reset(); myCourses = []; })
    .catch(function(){ showMsg('Failed to create course.', 'error'); });
});

// My Courses
function loadMyCourses() {
  // Fallback: at tab-click time GlideUser.js is guaranteed loaded
  if (!currentUser && typeof g_user !== 'undefined' && g_user.userID) {
    currentUser = g_user.userID;
    currentUserName = g_user.userName;
  }
  var el = document.getElementById('courses-list');
  el.innerHTML = '<div class="loading"><div class="spinner"></div>Loading...</div>';
  var q = 'created_by=' + currentUser;
  console.log('[LMS] loadMyCourses user:', currentUser, 'query:', q);
  fetch(API + '/' + SCOPE + '_courses?sysparm_query=' + encodeURIComponent(q) + '&sysparm_display_value=all&sysparm_limit=100', { headers: hdrs() })
    .then(function(r){ return r.json(); })
    .then(function(data) {
      myCourses = data.result || [];
      console.log('[LMS] courses returned:', myCourses.length, myCourses);
      populateGradingFilter(myCourses);
      if (!myCourses.length) {
        el.innerHTML = '<div class="empty-state"><div class="icon">&#128218;</div><p>No courses found.</p></div>';
        return;
      }
      var grid = document.createElement('div');
      grid.className = 'grid';
      myCourses.forEach(function(c) {
        var pub = c.published === true || c.published === 'true';
        var diff = getVal(c, 'difficulty_level');
        var cid = getVal(c, 'sys_id');
        var ctitle = getVal(c, 'title');
        var sel = selectedCourse && selectedCourse.id === cid;
        var card = document.createElement('div');
        card.className = 'course-card' + (sel ? ' selected' : '');
        card.innerHTML =
          '<div class="course-title">' + ctitle + '</div>' +
          '<div class="course-desc">' + getVal(c,'description') + '</div>' +
          '<div class="meta-row">' +
            '<span class="badge badge-' + diff + '">' + diff + '</span>' +
            '<span class="badge badge-' + (pub ? 'published' : 'draft') + '">' + (pub ? 'Published' : 'Draft') + '</span>' +
          '</div>' +
          '<div class="card-actions">' +
            '<button class="btn btn-primary btn-sm" onclick="selectCourse(\'' + cid + '\',\'' + escQ(ctitle) + '\')">Select</button>' +
            '<button class="btn btn-secondary btn-sm" onclick="openEditCourse(\'' + cid + '\')">Edit</button>' +
            '<button class="btn btn-secondary btn-sm" onclick="togglePub(\'' + cid + '\',' + !pub + ')">' + (pub ? 'Unpublish' : 'Publish') + '</button>' +
            '<a class="btn btn-secondary btn-sm" href="x_1879300_learning_lms.do?course=' + cid + '" target="_blank" style="text-decoration:none">&#128065; View in LMS</a>' +
          '</div>';
        grid.appendChild(card);
      });
      el.innerHTML = '';
      el.appendChild(grid);
    })
    .catch(function(){ el.innerHTML = '<div class="alert alert-error">Failed to load courses.</div>'; });
}

function togglePub(id, newPub) {
  fetch(API + '/' + SCOPE + '_courses/' + id, { method: 'PATCH', headers: hdrs(), body: JSON.stringify({ published: newPub }) })
    .then(function(){ loadMyCourses(); });
}

function openEditCourse(id) {
  var course = myCourses.find(function(c){ return getRawVal(c,'sys_id') === id || getVal(c,'sys_id') === id; });
  if (!course) return;
  var form = document.getElementById('form-edit-course');
  form.querySelector('[name=sys_id]').value = id;
  form.querySelector('[name=title]').value = getVal(course,'title');
  form.querySelector('[name=category]').value = getRawVal(course,'category');
  form.querySelector('[name=description]').value = getVal(course,'description');
  form.querySelector('[name=difficulty_level]').value = getRawVal(course,'difficulty_level');
  form.querySelector('[name=estimated_duration_hours]').value = getRawVal(course,'estimated_duration_hours') || getVal(course,'estimated_duration_hours');
  form.querySelector('[name=published]').checked = course.published === true || course.published === 'true';
  form.querySelector('[name=active]').checked = course.active === true || course.active === 'true';
  document.getElementById('edit-course-msg').innerHTML = '';
  document.getElementById('modal-overlay').classList.remove('hidden');
}

function closeEditCourse() {
  document.getElementById('modal-overlay').classList.add('hidden');
}

document.getElementById('form-edit-course').addEventListener('submit', function(e) {
  e.preventDefault();
  var fd = new FormData(e.target);
  var id = fd.get('sys_id');
  var body = {
    title: fd.get('title'),
    description: fd.get('description'),
    category: fd.get('category'),
    difficulty_level: fd.get('difficulty_level'),
    estimated_duration_hours: fd.get('estimated_duration_hours') || 0,
    published: fd.has('published'),
    active: fd.has('active')
  };
  fetch(API + '/' + SCOPE + '_courses/' + id, { method: 'PATCH', headers: hdrs(), body: JSON.stringify(body) })
    .then(function(r){ if (!r.ok) throw new Error(); closeEditCourse(); showMsg('Course updated!', 'success'); loadMyCourses(); })
    .catch(function(){ document.getElementById('edit-course-msg').innerHTML = '<div class="alert alert-error">Failed to update course.</div>'; });
});

function selectCourse(id, title) {
  selectedCourse = { id: id, title: title };
  var bannerHtml = '<span><strong>' + title + '</strong> selected</span><button class="btn btn-secondary btn-sm" onclick="clearCourse()">Change</button>';
  ['lessons','assess','staff'].forEach(function(p) {
    var b = document.getElementById(p + '-course-banner');
    var m = document.getElementById(p + '-main');
    if (b && m) { b.classList.add('hidden'); m.classList.remove('hidden'); }
    var sb = document.getElementById(p + '-selected-banner');
    if (sb) sb.innerHTML = bannerHtml;
  });
  loadLessons();
  loadAssessments();
  loadStaff();
  showMsg('Course "' + title + '" selected', 'success');
  loadMyCourses();
}

function clearCourse() {
  selectedCourse = null;
  ['lessons','assess','staff'].forEach(function(p) {
    var b = document.getElementById(p + '-course-banner');
    var m = document.getElementById(p + '-main');
    if (b && m) { b.classList.remove('hidden'); m.classList.add('hidden'); }
  });
}

// Lessons
document.getElementById('lesson-type-select').addEventListener('change', function() {
  var t = this.value;
  document.getElementById('lesson-content-group').classList.toggle('hidden', t === 'video');
  document.getElementById('lesson-video-group').classList.toggle('hidden', t !== 'video');
});

document.getElementById('form-lesson').addEventListener('submit', function(e) {
  e.preventDefault();
  if (!selectedCourse) return;
  document.getElementById('lesson-content-hidden').value = document.getElementById('lesson-content-editor').innerHTML;
  var fd = new FormData(e.target);
  var body = {
    title: fd.get('title'),
    course: selectedCourse.id,
    lesson_type: fd.get('lesson_type'),
    description: fd.get('description') || '',
    content: fd.get('content') || '',
    video_url: fd.get('video_url') || '',
    order_sequence: parseInt(fd.get('order_sequence')) || 1
  };
  var url = editingLessonId ? API + '/' + SCOPE + '_lessons/' + editingLessonId : API + '/' + SCOPE + '_lessons';
  var method = editingLessonId ? 'PATCH' : 'POST';
  fetch(url, { method: method, headers: hdrs(), body: JSON.stringify(body) })
    .then(function(r){ if (!r.ok) throw new Error(); showMsg(editingLessonId ? 'Lesson updated!' : 'Lesson added!', 'success'); cancelEditLesson(); loadLessons(); })
    .catch(function(){ showMsg('Failed to ' + (editingLessonId ? 'update' : 'add') + ' lesson.', 'error'); });
});

function loadLessons() {
  if (!selectedCourse) return;
  var el = document.getElementById('lessons-list');
  el.innerHTML = '<div class="loading"><div class="spinner"></div></div>';
  fetch(API + '/' + SCOPE + '_lessons?sysparm_query=course=' + selectedCourse.id + '^ORDERBYorder_sequence&sysparm_display_value=all&sysparm_limit=100', { headers: hdrs() })
    .then(function(r){ return r.json(); })
    .then(function(data) {
      lessonsList = data.result || [];
      updateDefaultOrder();
      if (!lessonsList.length) {
        el.innerHTML = '<div class="empty-state"><div class="icon">&#128196;</div><p>No lessons yet.</p></div>';
        return;
      }
      var html = '<table class="tbl"><thead><tr><th style="width:28px"></th><th style="width:36px">#</th><th>Title</th><th>Type</th><th></th></tr></thead><tbody id="lessons-tbody">';
      lessonsList.forEach(function(l) {
        var t = getVal(l,'lesson_type');
        var lid = getVal(l,'sys_id');
        html += '<tr draggable="true" data-lid="' + lid + '">' +
          '<td class="drag-handle" title="Drag to reorder">&#8942;</td>' +
          '<td class="order-cell">' + getVal(l,'order_sequence') + '</td>' +
          '<td>' + getVal(l,'title') + '</td>' +
          '<td><span class="badge badge-' + t + '">' + t + '</span></td>' +
          '<td style="white-space:nowrap">' +
          '<button class="btn btn-secondary btn-sm" style="margin-right:4px" onclick="editLesson(\'' + lid + '\')">Edit</button>' +
          '<button class="btn btn-danger btn-sm" onclick="deleteLesson(\'' + lid + '\')">Del</button></td></tr>';
      });
      el.innerHTML = html + '</tbody></table>';
      makeTableDraggable(document.getElementById('lessons-tbody'));
    })
    .catch(function(){ el.innerHTML = '<div class="alert alert-error">Failed to load lessons.</div>'; });
}

function editLesson(id) {
  var l = lessonsList.find(function(x){ return getVal(x,'sys_id') === id; });
  if (!l) return;
  editingLessonId = id;
  var form = document.getElementById('form-lesson');
  form.querySelector('[name=title]').value = getVal(l,'title');
  var typeSelect = document.getElementById('lesson-type-select');
  typeSelect.value = getRawVal(l,'lesson_type') || getVal(l,'lesson_type');
  typeSelect.dispatchEvent(new Event('change'));
  form.querySelector('[name=order_sequence]').value = getVal(l,'order_sequence');
  form.querySelector('[name=description]').value = getVal(l,'description');
  document.getElementById('lesson-content-editor').innerHTML = getVal(l,'content');
  form.querySelector('[name=video_url]').value = getVal(l,'video_url') || '';
  form.querySelector('button[type=submit]').textContent = 'Update Lesson';
  document.getElementById('cancel-edit-lesson').classList.remove('hidden');
  form.scrollIntoView({ behavior: 'smooth', block: 'start' });
}

function cancelEditLesson() {
  editingLessonId = null;
  document.getElementById('form-lesson').reset();
  document.getElementById('lesson-content-editor').innerHTML = '';
  document.getElementById('form-lesson').querySelector('button[type=submit]').textContent = 'Add Lesson';
  document.getElementById('cancel-edit-lesson').classList.add('hidden');
  document.getElementById('lesson-content-group').classList.remove('hidden');
  document.getElementById('lesson-video-group').classList.add('hidden');
  updateDefaultOrder();
}

function updateDefaultOrder() {
  if (editingLessonId) return;
  var maxOrder = 0;
  lessonsList.forEach(function(l) {
    var o = parseInt(getVal(l, 'order_sequence')) || 0;
    if (o > maxOrder) maxOrder = o;
  });
  var inp = document.getElementById('form-lesson').querySelector('[name=order_sequence]');
  if (inp) inp.value = maxOrder + 1;
}

function makeTableDraggable(tbody) {
  Array.from(tbody.querySelectorAll('tr[data-lid]')).forEach(function(row) {
    row.addEventListener('dragstart', function(e) {
      dragSrcRow = row;
      e.dataTransfer.effectAllowed = 'move';
      setTimeout(function(){ row.classList.add('dragging'); }, 0);
    });
    row.addEventListener('dragend', function() {
      row.classList.remove('dragging');
      Array.from(tbody.querySelectorAll('tr')).forEach(function(r){ r.classList.remove('drag-over'); });
    });
    row.addEventListener('dragover', function(e) {
      e.preventDefault();
      e.dataTransfer.dropEffect = 'move';
      if (row !== dragSrcRow) {
        Array.from(tbody.querySelectorAll('tr')).forEach(function(r){ r.classList.remove('drag-over'); });
        row.classList.add('drag-over');
      }
    });
    row.addEventListener('dragleave', function() {
      row.classList.remove('drag-over');
    });
    row.addEventListener('drop', function(e) {
      e.preventDefault();
      if (!dragSrcRow || dragSrcRow === row) return;
      var allRows = Array.from(tbody.querySelectorAll('tr[data-lid]'));
      var srcIdx = allRows.indexOf(dragSrcRow);
      var tgtIdx = allRows.indexOf(row);
      if (srcIdx < tgtIdx) {
        tbody.insertBefore(dragSrcRow, row.nextSibling);
      } else {
        tbody.insertBefore(dragSrcRow, row);
      }
      row.classList.remove('drag-over');
      saveNewOrder(tbody);
    });
  });
}

function saveNewOrder(tbody) {
  var allRows = Array.from(tbody.querySelectorAll('tr[data-lid]'));
  var patches = [];
  allRows.forEach(function(row, i) {
    var lid = row.dataset.lid;
    var newOrder = i + 1;
    var orderCell = row.querySelector('.order-cell');
    if (orderCell) orderCell.textContent = newOrder;
    var lesson = lessonsList.find(function(l){ return getVal(l, 'sys_id') === lid; });
    var oldOrder = lesson ? (parseInt(getVal(lesson, 'order_sequence')) || 0) : -1;
    if (oldOrder !== newOrder) {
      patches.push(fetch(API + '/' + SCOPE + '_lessons/' + lid, { method: 'PATCH', headers: hdrs(), body: JSON.stringify({ order_sequence: newOrder }) }));
    }
  });
  if (patches.length) {
    Promise.all(patches).then(function(){ loadLessons(); });
  }
}

function deleteLesson(id) {
  if (!confirm('Delete this lesson?')) return;
  fetch(API + '/' + SCOPE + '_lessons/' + id, { method: 'DELETE', headers: hdrs() })
    .then(function(){ loadLessons(); });
}

// Assessments
function showAssessPanel(panel) {
  ['list','create','questions'].forEach(function(p) {
    document.getElementById('assess-panel-' + p).classList.add('hidden');
    var pill = document.getElementById('pill-' + p);
    if (pill) pill.classList.remove('active');
  });
  document.getElementById('assess-panel-' + panel).classList.remove('hidden');
  var activePill = document.getElementById('pill-' + panel);
  if (activePill) activePill.classList.add('active');
}

document.getElementById('form-assessment').addEventListener('submit', function(e) {
  e.preventDefault();
  if (!selectedCourse) return;
  var fd = new FormData(e.target);
  var body = {
    title: fd.get('title'),
    course: selectedCourse.id,
    assessment_type: fd.get('assessment_type'),
    passing_score: parseInt(fd.get('passing_score')) || 70,
    time_limit_minutes: parseInt(fd.get('time_limit_minutes')) || 0,
    max_attempts: parseInt(fd.get('max_attempts')) || 3,
    show_results_immediately: fd.has('show_results_immediately')
  };
  fetch(API + '/' + SCOPE + '_assessments', { method: 'POST', headers: hdrs(), body: JSON.stringify(body) })
    .then(function(r){ if (!r.ok) throw new Error(); return r.json(); })
    .then(function(data) {
      var created = data.result;
      showMsg('Assessment created!', 'success');
      e.target.reset();
      openQuestionBuilder(getVal(created,'sys_id'), getVal(created,'title'));
      loadAssessments();
    })
    .catch(function(){ showMsg('Failed to create assessment.', 'error'); });
});

function loadAssessments() {
  if (!selectedCourse) return;
  var el = document.getElementById('assessments-list');
  el.innerHTML = '<div class="loading"><div class="spinner"></div></div>';
  fetch(API + '/' + SCOPE + '_assessments?sysparm_query=course=' + selectedCourse.id + '&sysparm_display_value=all&sysparm_limit=50', { headers: hdrs() })
    .then(function(r){ return r.json(); })
    .then(function(data) {
      var items = data.result || [];
      if (!items.length) {
        el.innerHTML = '<div class="empty-state"><div class="icon">&#128203;</div><p>No assessments yet.</p></div>';
        return;
      }
      var html = '<table class="tbl"><thead><tr><th>Title</th><th>Type</th><th>Passing%</th><th>Attempts</th><th></th></tr></thead><tbody>';
      items.forEach(function(a) {
        html += '<tr><td>' + getVal(a,'title') + '</td><td>' + getVal(a,'assessment_type') + '</td>' +
          '<td>' + getVal(a,'passing_score') + '</td><td>' + getVal(a,'max_attempts') + '</td><td>' +
          '<button class="btn btn-primary btn-sm" onclick="openQuestionBuilder(\'' + getVal(a,'sys_id') + '\',\'' + escQ(getVal(a,'title')) + '\')">Questions</button> ' +
          '<button class="btn btn-danger btn-sm" onclick="deleteAssessment(\'' + getVal(a,'sys_id') + '\')">Del</button></td></tr>';
      });
      el.innerHTML = html + '</tbody></table>';
    })
    .catch(function(){ el.innerHTML = '<div class="alert alert-error">Failed to load.</div>'; });
}

function deleteAssessment(id) {
  if (!confirm('Delete this assessment?')) return;
  fetch(API + '/' + SCOPE + '_assessments/' + id, { method: 'DELETE', headers: hdrs() })
    .then(function(){ loadAssessments(); });
}

function openQuestionBuilder(aid, atitle) {
  selectedAssessment = { id: aid, title: atitle };
  document.getElementById('questions-assess-banner').innerHTML = '<span>Questions for: <strong>' + atitle + '</strong></span>';
  document.getElementById('pill-questions').classList.remove('hidden');
  showAssessPanel('questions');
  loadQuestions();
}

// Questions
document.getElementById('q-type').addEventListener('change', function() {
  var t = this.value;
  document.getElementById('q-choices-block').classList.toggle('hidden', t !== 'multiple_choice');
  document.getElementById('q-tf-block').classList.toggle('hidden', t !== 'true_false');
  document.getElementById('q-text-block').classList.toggle('hidden', t === 'multiple_choice' || t === 'true_false');
});

function addChoice() {
  var list = document.getElementById('q-choices-list');
  var idx = list.children.length;
  var labels = ['A','B','C','D','E','F','G','H'];
  var row = document.createElement('div');
  row.className = 'choice-row';
  row.innerHTML = '<input type="radio" name="correct_choice" value="' + idx + '" /><input type="text" class="choice-text" placeholder="Choice ' + (labels[idx] || idx) + '" /><button type="button" class="btn btn-danger btn-sm" onclick="removeChoice(this)">X</button>';
  list.appendChild(row);
}

function removeChoice(btn) { btn.closest('.choice-row').remove(); }

document.getElementById('form-question').addEventListener('submit', function(e) {
  e.preventDefault();
  if (!selectedAssessment) return;
  var fd = new FormData(e.target);
  var qtype = fd.get('question_type');
  var body = {
    assessment: selectedAssessment.id,
    question_type: qtype,
    question_text: fd.get('question_text'),
    points: parseInt(fd.get('points')) || 1,
    order_sequence: parseInt(fd.get('order_sequence')) || 1,
    hint: fd.get('hint') || ''
  };
  if (qtype === 'true_false') body.correct_answer_text = fd.get('tf_answer');
  if (qtype === 'short_answer' || qtype === 'essay') body.correct_answer_text = fd.get('correct_answer_text') || '';

  fetch(API + '/' + SCOPE + '_questions', { method: 'POST', headers: hdrs(), body: JSON.stringify(body) })
    .then(function(r){ if (!r.ok) throw new Error(); return r.json(); })
    .then(function(data) {
      var qid = getVal(data.result, 'sys_id');
      if (qtype === 'multiple_choice') {
        var correctIdx = parseInt(fd.get('correct_choice') || '0');
        var choiceEls = document.querySelectorAll('.choice-text');
        var labels = ['A','B','C','D','E','F'];
        var promises = [];
        choiceEls.forEach(function(inp, i) {
          if (!inp.value.trim()) return;
          promises.push(fetch(API + '/' + SCOPE + '_question_choices', { method: 'POST', headers: hdrs(), body: JSON.stringify({
            question: qid, choice_text: inp.value, choice_label: labels[i] || String(i+1), is_correct: i === correctIdx, order_sequence: i+1
          })}));
        });
        return Promise.all(promises);
      }
    })
    .then(function(){
      showMsg('Question added!', 'success');
      e.target.reset();
      loadQuestions();
    })
    .catch(function(){ showMsg('Failed to add question.', 'error'); });
});

function loadQuestions() {
  if (!selectedAssessment) return;
  var el = document.getElementById('questions-list');
  el.innerHTML = '<div class="loading"><div class="spinner"></div></div>';
  fetch(API + '/' + SCOPE + '_questions?sysparm_query=assessment=' + selectedAssessment.id + '^ORDERBYorder_sequence&sysparm_display_value=all&sysparm_limit=50', { headers: hdrs() })
    .then(function(r){ return r.json(); })
    .then(function(data) {
      var qs = data.result || [];
      if (!qs.length) {
        el.innerHTML = '<div class="empty-state"><div class="icon">&#10067;</div><p>No questions yet.</p></div>';
        return;
      }
      var html = '';
      qs.forEach(function(q, i) {
        var qt = getVal(q,'question_type');
        html += '<div class="question-block"><strong>' + (i+1) + '. [' + qt + ']</strong> ' + getVal(q,'question_text') +
          ' <span style="color:#a0aec0;font-size:.8rem">(' + getVal(q,'points') + ' pt)</span>' +
          '<div style="margin-top:8px"><button class="btn btn-danger btn-sm" onclick="deleteQuestion(\'' + getVal(q,'sys_id') + '\')">Delete</button></div></div>';
      });
      el.innerHTML = html;
    })
    .catch(function(){ el.innerHTML = '<div class="alert alert-error">Failed to load.</div>'; });
}

function deleteQuestion(id) {
  if (!confirm('Delete this question?')) return;
  fetch(API + '/' + SCOPE + '_questions/' + id, { method: 'DELETE', headers: hdrs() })
    .then(function(){ loadQuestions(); });
}

// Staff
document.getElementById('form-staff').addEventListener('submit', function(e) {
  e.preventDefault();
  if (!selectedCourse) return;
  var fd = new FormData(e.target);
  var body = { course: selectedCourse.id, user: fd.get('user'), staff_role: fd.get('staff_role'), active: true, added_by: currentUser };
  fetch(API + '/' + SCOPE + '_course_staff', { method: 'POST', headers: hdrs(), body: JSON.stringify(body) })
    .then(function(r){ if (!r.ok) throw new Error(); showMsg('Staff member added!', 'success'); e.target.reset(); loadStaff(); })
    .catch(function(){ showMsg('Failed to add staff.', 'error'); });
});

function loadStaff() {
  if (!selectedCourse) return;
  var el = document.getElementById('staff-list');
  el.innerHTML = '<div class="loading"><div class="spinner"></div></div>';
  fetch(API + '/' + SCOPE + '_course_staff?sysparm_query=course=' + selectedCourse.id + '^active=true&sysparm_display_value=all&sysparm_limit=50', { headers: hdrs() })
    .then(function(r){ return r.json(); })
    .then(function(data) {
      var items = data.result || [];
      if (!items.length) {
        el.innerHTML = '<div class="empty-state"><div class="icon">&#128100;</div><p>No staff assigned.</p></div>';
        return;
      }
      var html = '<table class="tbl"><thead><tr><th>User</th><th>Role</th><th>Added</th><th></th></tr></thead><tbody>';
      items.forEach(function(s) {
        html += '<tr><td>' + getVal(s,'user') + '</td><td>' + getVal(s,'staff_role') + '</td>' +
          '<td>' + (getVal(s,'added_date') || '').split(' ')[0] + '</td>' +
          '<td><button class="btn btn-danger btn-sm" onclick="removeStaff(\'' + getVal(s,'sys_id') + '\')">Remove</button></td></tr>';
      });
      el.innerHTML = html + '</tbody></table>';
    })
    .catch(function(){ el.innerHTML = '<div class="alert alert-error">Failed to load staff.</div>'; });
}

function removeStaff(id) {
  if (!confirm('Remove this staff member?')) return;
  fetch(API + '/' + SCOPE + '_course_staff/' + id, { method: 'PATCH', headers: hdrs(), body: JSON.stringify({ active: false }) })
    .then(function(){ loadStaff(); });
}

// Grading
function populateGradingFilter(courses) {
  var sel = document.getElementById('grading-course-filter');
  while (sel.options.length > 1) sel.remove(1);
  (courses || []).forEach(function(c) {
    var opt = new Option(getVal(c,'title'), getVal(c,'sys_id'));
    sel.add(opt);
  });
}

function loadSubmissions() {
  var el = document.getElementById('submissions-list');
  el.innerHTML = '<div class="loading"><div class="spinner"></div>Loading...</div>';
  var courseId = document.getElementById('grading-course-filter').value;
  var status = document.getElementById('grading-status-filter').value;
  var query = '';
  if (status) query += 'status=' + status + '^';
  if (courseId) query += 'lesson.course=' + courseId + '^';
  query += 'ORDERBYDESCsubmitted_at';
  fetch(API + '/' + SCOPE + '_student_submission?sysparm_query=' + encodeURIComponent(query) + '&sysparm_display_value=all&sysparm_limit=50', { headers: hdrs() })
    .then(function(r){ return r.json(); })
    .then(function(data) {
      var subs = data.result || [];
      if (!subs.length) {
        el.innerHTML = '<div class="empty-state"><div class="icon">&#10003;</div><p>No submissions found.</p></div>';
        return;
      }
      var html = '';
      subs.forEach(function(s) {
        var sid = getVal(s,'sys_id');
        var st = getVal(s,'status');
        html += '<div class="sub-card">' +
          '<strong>' + getVal(s,'student') + '</strong> — Lesson: ' + getVal(s,'lesson') +
          ' <span class="badge" style="background:#e2e8f0;color:#4a5568">' + st + '</span><br/>' +
          '<em style="font-size:.85rem;color:#718096">Submitted: ' + (getVal(s,'submitted_at') || '').split(' ')[0] + '</em>';
        if (getVal(s,'student_notes')) html += '<p style="margin:8px 0;font-size:.9rem;color:#4a5568">' + getVal(s,'student_notes') + '</p>';
        if (st !== 'graded') {
          html += '<div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:10px">' +
            '<div class="form-group" style="margin:0"><label style="font-size:.8rem">Grade (0-100)</label><input type="number" id="gv-' + sid + '" min="0" max="100" placeholder="Score" /></div>' +
            '<div class="form-group" style="margin:0"><label style="font-size:.8rem">Feedback</label><input type="text" id="gf-' + sid + '" placeholder="Feedback for student" /></div>' +
            '</div><button class="btn btn-primary btn-sm" style="margin-top:8px" onclick="submitGrade(\'' + sid + '\')">Submit Grade</button>';
        } else {
          html += '<p style="margin-top:6px;font-size:.85rem">Grade: <strong>' + getVal(s,'grade') + '</strong> | ' + (getVal(s,'instructor_feedback') || '') + '</p>';
        }
        html += '</div>';
      });
      el.innerHTML = html;
    })
    .catch(function(){ el.innerHTML = '<div class="alert alert-error">Failed to load submissions.</div>'; });
}

function submitGrade(sid) {
  var grade = document.getElementById('gv-' + sid).value;
  var fb = document.getElementById('gf-' + sid).value;
  if (!grade) { alert('Enter a grade.'); return; }
  var now = new Date().toISOString().replace('T',' ').split('.')[0];
  fetch(API + '/' + SCOPE + '_student_submission/' + sid, { method: 'PATCH', headers: hdrs(), body: JSON.stringify({
    grade: parseFloat(grade), instructor_feedback: fb, status: 'graded', graded_by: currentUser, graded_at: now
  })})
    .then(function(){ showMsg('Grade submitted!', 'success'); loadSubmissions(); })
    .catch(function(){ showMsg('Failed to submit grade.', 'error'); });
}

// currentUser and currentUserName are set at declaration time from server-embedded values
</script>
</body>
</html>]]></html>
        <name>instructor_dashboard</name>
        <processing_script/>
        <sys_class_name>sys_ui_page</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2025-12-22 16:37:20</sys_created_on>
        <sys_id>0f3a5b3148fa4495a58599ef0522dd4c</sys_id>
        <sys_mod_count>2</sys_mod_count>
        <sys_name>instructor_dashboard</sys_name>
        <sys_package display_value="Learning Man" source="x_1879300_learning">95470fa283a5f21071a1ba96feaad3cb</sys_package>
        <sys_policy/>
        <sys_scope display_value="Learning Man">95470fa283a5f21071a1ba96feaad3cb</sys_scope>
        <sys_update_name>sys_ui_page_0f3a5b3148fa4495a58599ef0522dd4c</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2026-04-28 00:00:00</sys_updated_on>
    </sys_ui_page>
</record_update>
