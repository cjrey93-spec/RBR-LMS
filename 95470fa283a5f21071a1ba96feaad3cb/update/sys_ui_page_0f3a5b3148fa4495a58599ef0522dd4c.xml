<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_ui_page">
    <sys_ui_page action="INSERT_OR_UPDATE">
        <category>general</category>
        <client_script/>
        <description>Instructor interface for creating and managing courses, lessons, and assessments</description>
        <direct>true</direct>
        <endpoint>x_1879300_learning_instructor_dashboard.do</endpoint>
        <html><![CDATA[<html>
  <head>
    <title>Instructor Dashboard - Learning Management System</title>
    <meta charset="UTF-8"></meta>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"></meta>
    <!-- ServiceNow globals and scripts -->
    <!-- @sdk:now-ux-globals -->
    <!-- Resolved from sdk:now-ux-globals tag at NowSDK build time to support earlier Glide releases -->
    <script>window.NOW = {};
window.NOW.user = {};
window.NOW.batch_glide_ajax_requests = false;
window.NOW.isUsingPolaris = true;
window.NOW.exclude_dark_theme = "false";
window.g_ck = "$[gs.getSession().getSessionToken() || gs.getSessionToken()]";</script>
    <!-- Include ServiceNow's required scripts -->
    <g:requires name="scripts/doctype/functions_bootstrap14.js"></g:requires>
    <g:requires name="scripts/lib/prototype.js"></g:requires>
    <g:requires name="scripts/classes/ajax/GlideURL.js"></g:requires>
    <g:requires name="scripts/doctype/CustomEventManager.js"></g:requires>
    <g:requires name="scripts/classes/ajax/GlideAjax.js"></g:requires>
    <g:requires name="scripts/classes/GlideUser.js"></g:requires>
    <g2:client_script type="user"></g2:client_script>
    <link rel="preload" href="/uxasset/set-cache-buster/$[UxFrameworkScriptables.getFlushTimestamp()].js" as="script"></link>
    <g:requires name="scripts/polaris_theme_refresh_observer.js"></g:requires>
    <link data-source-id="glide-theme" id="polarisberg_theme_variables" rel="stylesheet" href="/$uxappimmutables.do?sysparm_request_type=ux_theme$[AMP]sysparm_app_sys_id=c86a62e2c7022010099a308dc7c26022$[AMP]uxpcb=$[sn_ui.PolarisUI.getThemeVariableCssCacheKey()]"></link>
    <script type="module" src="/uxasset/externals/@devsnc/library-uxf/index.jsdbx"></script>
    <!-- @sdk:now-ux-globals -->
    <style>* {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
    }
    
    .dashboard-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
    }
    
    .dashboard-header {
      text-align: center;
      color: white;
      margin-bottom: 30px;
    }
    
    .dashboard-header h1 {
      font-size: 2.5rem;
      margin-bottom: 10px;
    }
    
    .dashboard-tabs {
      display: flex;
      justify-content: center;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      padding: 8px;
      margin-bottom: 30px;
      backdrop-filter: blur(10px);
    }
    
    .tab-btn {
      padding: 12px 24px;
      border: none;
      background: transparent;
      color: white;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    
    .tab-btn:hover {
      background: rgba(255, 255, 255, 0.1);
    }
    
    .tab-btn.active {
      background: white;
      color: #667eea;
      font-weight: 600;
    }
    
    .dashboard-content {
      background: white;
      border-radius: 16px;
      padding: 30px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
      min-height: 500px;
    }
    
    .section-title {
      font-size: 1.8rem;
      color: #2d3748;
      margin-bottom: 20px;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    
    .form-group {
      margin-bottom: 20px;
    }
    
    .form-group label {
      display: block;
      margin-bottom: 5px;
      font-weight: 600;
      color: #4a5568;
    }
    
    .form-group input,
    .form-group textarea,
    .form-group select {
      width: 100%;
      padding: 12px;
      border: 2px solid #e2e8f0;
      border-radius: 8px;
      font-size: 1rem;
      transition: border-color 0.3s ease;
    }
    
    .form-group input:focus,
    .form-group textarea:focus,
    .form-group select:focus {
      outline: none;
      border-color: #667eea;
    }
    
    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
    }
    
    .btn {
      padding: 12px 24px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1rem;
      font-weight: 600;
      transition: all 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }
    
    .btn-primary {
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
    }
    
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
    }
    
    .btn-secondary {
      background: #f7fafc;
      color: #4a5568;
      border: 2px solid #e2e8f0;
    }
    
    .btn-secondary:hover {
      background: #edf2f7;
    }
    
    .courses-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 20px;
      margin-top: 20px;
    }
    
    .course-card {
      background: #f7fafc;
      border: 2px solid #e2e8f0;
      border-radius: 12px;
      padding: 20px;
      transition: all 0.3s ease;
    }
    
    .course-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    }
    
    .course-title {
      font-size: 1.2rem;
      font-weight: 600;
      color: #2d3748;
      margin-bottom: 8px;
    }
    
    .course-meta {
      display: flex;
      gap: 10px;
      margin: 10px 0;
    }
    
    .badge {
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 0.875rem;
      font-weight: 600;
    }
    
    .badge-beginner { background: #bee3f8; color: #2b6cb0; }
    .badge-intermediate { background: #fbb6ce; color: #b83280; }
    .badge-advanced { background: #fbd38d; color: #c05621; }
    .badge-published { background: #c6f6d5; color: #22543d; }
    .badge-draft { background: #fed7d7; color: #c53030; }
    
    .loading {
      text-align: center;
      padding: 40px;
      color: #718096;
    }
    
    .error {
      background: #fed7d7;
      color: #c53030;
      padding: 15px;
      border-radius: 8px;
      margin-bottom: 20px;
    }
    
    .success {
      background: #c6f6d5;
      color: #22543d;
      padding: 15px;
      border-radius: 8px;
      margin-bottom: 20px;
    }
    
    .hidden {
      display: none !important;
    }
    
    .checkbox-group {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-top: 10px;
    }
    
    .checkbox-group input[type="checkbox"] {
      width: auto;
    }</style>
  </head>
  <body>
    <div class="dashboard-container">
      <header class="dashboard-header">
        <h1>]]>👩<![CDATA[‍]]>🏫<![CDATA[ Instructor Dashboard</h1>
        <p>Create and manage your courses, lessons, and assessments</p>
      </header>
      <nav class="dashboard-tabs">
        <button class="tab-btn active" data-tab="create">
          
          <span>]]>🆕<![CDATA[</span>
          Create Course
        </button>
        <button class="tab-btn" data-tab="manage">
          
          <span>]]>📚<![CDATA[</span>
          My Courses
        </button>
        <button class="tab-btn" data-tab="lessons">
          
          <span>]]>📝<![CDATA[</span>
          Manage Lessons
        </button>
        <button class="tab-btn" data-tab="assessments">
          
          <span>❓</span>
          Create Assessments
        </button>
      </nav>
      <main class="dashboard-content">
        <!-- Create Course Tab -->
        <div id="create-tab" class="tab-content">
          <h2 class="section-title">]]>🆕<![CDATA[ Create New Course</h2>
          <div id="course-message"></div>
          <form id="course-form">
            <div class="form-group">
              <label for="course-title">Course Title *</label>
              <input type="text" id="course-title" name="title"></input>
              <div class="form-group">
                <label for="course-description">Course Description *</label>
                <textarea id="course-description" name="description" rows="4"></textarea>
              </div>
              <div class="form-row">
                <div class="form-group">
                  <label for="course-category">Category *</label>
                  <select id="course-category" name="category">
                    <option value="">Select Category</option>
                    <option value="programming">Programming</option>
                    <option value="web_development">Web Development</option>
                    <option value="data_science">Data Science</option>
                    <option value="devops">DevOps $[AMP] Infrastructure</option>
                    <option value="mobile">Mobile Development</option>
                    <option value="other">Other</option>
                  </select>
                </div>
                <div class="form-group">
                  <label for="course-difficulty">Difficulty Level *</label>
                  <select id="course-difficulty" name="difficulty">
                    <option value="">Select Difficulty</option>
                    <option value="beginner">Beginner</option>
                    <option value="intermediate">Intermediate</option>
                    <option value="advanced">Advanced</option>
                  </select>
                </div>
              </div>
              <div class="form-row">
                <div class="form-group">
                  <label for="course-duration">Estimated Duration (hours)</label>
                  <input type="number" id="course-duration" name="duration" min="1" max="200"></input>
                  <div class="form-group">
                    <label>$[AMP]nbsp;</label>
                    <div class="checkbox-group">
                      <input type="checkbox" id="course-published" name="published">
                        <label for="course-published">Publish immediately</label>
                      </input>
                    </div>
                  </div>
                  <button type="submit" class="btn btn-primary">
                    
                    <span>✨</span>
                    Create Course
                  </button>
                </div>
              </div>
              <!-- My Courses Tab -->
              <div id="manage-tab" class="tab-content hidden">
                <h2 class="section-title">]]>📚<![CDATA[ My Courses</h2>
                <div id="courses-loading" class="loading">
                  <div>Loading your courses...</div>
                </div>
                <div id="courses-error" class="error hidden"></div>
                <div id="courses-grid" class="courses-grid"></div>
              </div>
              <!-- Manage Lessons Tab -->
              <div id="lessons-tab" class="tab-content hidden">
                <h2 class="section-title">]]>📝<![CDATA[ Manage Lessons</h2>
                <div id="selected-course-info"></div>
                <p>Lesson management interface coming soon. Select a course from "My Courses" tab first.</p>
              </div>
              <!-- Create Assessments Tab -->
              <div id="assessments-tab" class="tab-content hidden">
                <h2 class="section-title">❓ Create Assessments</h2>
                <div id="selected-course-info-assessments"></div>
                <p>Assessment creation interface coming soon. Select a course from "My Courses" tab first.</p>
              </div>
            </div>
          </form>
          <script>
            // Global variables
    let selectedCourse = null;
    
    // API configuration
    const API_BASE = '/api/now/table';
    const SCOPE = 'x_1879300_learning';
    
    // Get headers for ServiceNow API
    function getHeaders() {
      return {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        'X-UserToken': window.g_ck
      };
    }
    
    // Show message
    function showMessage(message, type = 'success') {
      const messageDiv = document.getElementById('course-message');
      messageDiv.innerHTML = `
            <div class="${type}">${message}</div>
            `;
      setTimeout(() => {
        messageDiv.innerHTML = '';
      }, 5000);
    }
    
    // Handle tab switching
    function setupTabs() {
      const tabButtons = document.querySelectorAll('.tab-btn');
      const tabContents = document.querySelectorAll('.tab-content');
      
      tabButtons.forEach(button => {
        button.addEventListener('click', () => {
          const tabName = button.dataset.tab;
          
          // Update active tab
          tabButtons.forEach(btn => btn.classList.remove('active'));
          button.classList.add('active');
          
          // Show/hide content
          tabContents.forEach(content => {
            content.classList.add('hidden');
          });
          document.getElementById(`${tabName}-tab`).classList.remove('hidden');
          
          // Load data for specific tabs
          if (tabName === 'manage') {
            loadMyCourses();
          }
        });
      });
    }
    
    // Handle course form submission
    function setupCourseForm() {
      const form = document.getElementById('course-form');
      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        
        const formData = new FormData(form);
        const courseData = {
          title: formData.get('title'),
          description: formData.get('description'),
          category: formData.get('category'),
          difficulty: formData.get('difficulty'),
          estimated_duration: formData.get('duration') || 0,
          published: formData.has('published'),
          active: true,
          instructor: window.g_user.userID
        };
        
        try {
          const response = await fetch(`${API_BASE}/${SCOPE}_courses`, {
            method: 'POST',
            headers: getHeaders(),
            body: JSON.stringify(courseData)
          });
          
          if (response.ok) {
            showMessage('✅ Course created successfully!', 'success');
            form.reset();
            // Refresh courses if on manage tab
            if (document.querySelector('.tab-btn[data-tab="manage"]').classList.contains('active')) {
              loadMyCourses();
            }
          } else {
            throw new Error('Failed to create course');
          }
        } catch (error) {
          console.error('Error creating course:', error);
          showMessage('❌ Failed to create course. Please try again.', 'error');
        }
      });
    }
    
    // Load instructor's courses
    async function loadMyCourses() {
      const loadingDiv = document.getElementById('courses-loading');
      const errorDiv = document.getElementById('courses-error');
      const gridDiv = document.getElementById('courses-grid');
      
      loadingDiv.classList.remove('hidden');
      errorDiv.classList.add('hidden');
      gridDiv.innerHTML = '';
      
      try {
        const response = await fetch(
          `${API_BASE}/${SCOPE}_courses?sysparm_query=instructor=${window.g_user.userID}$[AMP]sysparm_display_value=all`,
          { headers: getHeaders() }
        );
        
        if (!response.ok) {
          throw new Error('Failed to load courses');
        }
        
        const data = await response.json();
        const courses = data.result || [];
        
        loadingDiv.classList.add('hidden');
        
        if (courses.length === 0) {
          gridDiv.innerHTML = '
            <div class="loading">]]>📚<![CDATA[ No courses created yet. Create your first course!</div>
            ';
          return;
        }
        
        courses.forEach(course => {
          const courseCard = createCourseCard(course);
          gridDiv.appendChild(courseCard);
        });
        
      } catch (error) {
        console.error('Error loading courses:', error);
        loadingDiv.classList.add('hidden');
        errorDiv.textContent = 'Failed to load courses. Please try again.';
        errorDiv.classList.remove('hidden');
      }
    }
    
    // Create course card element
    function createCourseCard(course) {
      const card = document.createElement('div');
      card.className = 'course-card';
      
      const title = typeof course.title === 'object' ? course.title.display_value : course.title;
      const description = typeof course.description === 'object' ? course.description.display_value : course.description;
      const difficulty = typeof course.difficulty === 'object' ? course.difficulty.display_value : course.difficulty;
      const published = course.published === true || course.published === 'true';
      
      card.innerHTML = `
            <div class="course-title">${title}</div>
            <div class="course-description">${description}</div>
            <div class="course-meta">
              <span class="badge badge-${difficulty}">${difficulty}</span>
              <span class="badge badge-${published ? 'published' : 'draft'}">${published ? 'Published' : 'Draft'}</span>
            </div>
            <div style="margin-top: 15px; display: flex; gap: 10px;">
              <button class="btn btn-primary" onclick="selectCourse('${course.sys_id}', '${title}')">]]>📝<![CDATA[ Manage</button>
              <button class="btn btn-secondary" onclick="togglePublished('${course.sys_id}', ${!published})">${published ? ']]>📋<![CDATA[ Unpublish' : ']]>🚀<![CDATA[ Publish'}</button>
            </div>
            `;
      
      return card;
    }
    
    // Select course for lesson/assessment management
    window.selectCourse = function(courseId, courseTitle) {
      selectedCourse = { id: courseId, title: courseTitle };
      
      // Update lesson and assessment tabs
      const lessonInfo = document.getElementById('selected-course-info');
      const assessmentInfo = document.getElementById('selected-course-info-assessments');
      
      const infoHTML = `
            <div class="success">]]>📚<![CDATA[ Selected Course:
              <strong>${courseTitle}</strong>
            </div>
            `;
      lessonInfo.innerHTML = infoHTML;
      assessmentInfo.innerHTML = infoHTML;
      
      showMessage(`✅ Course "${courseTitle}" selected for management`, 'success');
    };
    
    // Toggle course published status
    window.togglePublished = async function(courseId, newStatus) {
      try {
        const response = await fetch(`${API_BASE}/${SCOPE}_courses/${courseId}`, {
          method: 'PATCH',
          headers: getHeaders(),
          body: JSON.stringify({ published: newStatus })
        });
        
        if (response.ok) {
          showMessage(`✅ Course ${newStatus ? 'published' : 'unpublished'} successfully!`, 'success');
          loadMyCourses(); // Refresh the courses grid
        } else {
          throw new Error('Failed to update course');
        }
      } catch (error) {
        console.error('Error updating course:', error);
        showMessage('❌ Failed to update course status', 'error');
      }
    };
    
    // Initialize the dashboard
    document.addEventListener('DOMContentLoaded', () => {
      console.log('Instructor Dashboard initialized');
      setupTabs();
      setupCourseForm();
    });
          </script>
        </div>
      </main>
    </div>
  </body>
</html>]]></html>
        <name>instructor_dashboard</name>
        <processing_script/>
        <sys_class_name>sys_ui_page</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2025-12-22 16:37:20</sys_created_on>
        <sys_id>0f3a5b3148fa4495a58599ef0522dd4c</sys_id>
        <sys_mod_count>1</sys_mod_count>
        <sys_name>instructor_dashboard</sys_name>
        <sys_package display_value="Learning Man" source="x_1879300_learning">95470fa283a5f21071a1ba96feaad3cb</sys_package>
        <sys_policy/>
        <sys_scope display_value="Learning Man">95470fa283a5f21071a1ba96feaad3cb</sys_scope>
        <sys_update_name>sys_ui_page_0f3a5b3148fa4495a58599ef0522dd4c</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2026-02-14 06:27:24</sys_updated_on>
    </sys_ui_page>
</record_update>
