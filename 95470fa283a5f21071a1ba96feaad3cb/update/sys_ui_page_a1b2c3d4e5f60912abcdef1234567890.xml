<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_ui_page">
    <sys_ui_page action="INSERT_OR_UPDATE">
        <category>general</category>
        <client_script/>
        <description>Admin dashboard for managing all LMS data: courses, lessons, enrollments, assessments, and users</description>
        <direct>true</direct>
        <endpoint>x_1879300_learning_admin_dashboard.do</endpoint>
        <html><![CDATA[<html>
<head>
  <title>Admin Dashboard - LMS</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <g:evaluate var="jvar_uid" expression="gs.getUserID()" />
  <g:evaluate var="jvar_uname" expression="gs.getUserName()" />
  <g:evaluate var="jvar_token" expression="gs.getSession().getSessionToken()" />
  <script>window.NOW={};window.NOW.user={};window.SN_USER_ID='${jvar_uid}';window.SN_USER_NAME='${jvar_uname}';window.SN_TOKEN='${jvar_token}';</script>
  <script>(function(){if(localStorage.getItem('lms-theme')==='dark')document.documentElement.setAttribute('data-theme','dark')})()</script>
  <g:requires name="scripts/doctype/functions_bootstrap14.js" />
  <g:requires name="scripts/lib/prototype.js" />
  <g:requires name="scripts/classes/ajax/GlideURL.js" />
  <g:requires name="scripts/doctype/CustomEventManager.js" />
  <g:requires name="scripts/classes/ajax/GlideAjax.js" />
  <g:requires name="scripts/classes/GlideUser.js" />
  <g2:client_script type="user" />
  <style>
    *{margin:0;padding:0;box-sizing:border-box}
    body{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif;background:#f0f2f8;min-height:100vh}
    .topbar{background:linear-gradient(135deg,#e53e3e 0%,#c53030 100%);padding:14px 28px;display:flex;justify-content:space-between;align-items:center;position:relative}
    .topbar-brand{color:#fff;font-size:1.05rem;font-weight:700;display:flex;align-items:center;gap:8px}
    .topbar-badge{background:rgba(255,255,255,.25);border-radius:20px;padding:2px 10px;font-size:.75rem;font-weight:700;letter-spacing:.05em}
    .topbar-nav{display:flex;gap:10px;align-items:center}
    .topbar-nav a{color:rgba(255,255,255,.85);text-decoration:none;font-size:.82rem;padding:5px 13px;border-radius:20px;border:1px solid rgba(255,255,255,.35)}
    .topbar-nav a:hover{background:rgba(255,255,255,.15)}
    .hero{background:linear-gradient(135deg,#e53e3e 0%,#c53030 100%);padding:24px 32px 48px;color:#fff}
    .hero h1{font-size:1.8rem;font-weight:800;margin-bottom:4px}
    .hero p{opacity:.8;font-size:.95rem}
    .stats-row{display:grid;grid-template-columns:repeat(5,1fr);gap:14px;max-width:1300px;margin:-28px auto 0;padding:0 28px}
    .stat-card{background:#fff;border-radius:12px;padding:18px 20px;box-shadow:0 4px 16px rgba(0,0,0,.1);text-align:center}
    .stat-num{font-size:2rem;font-weight:800;color:#e53e3e}
    .stat-label{font-size:.8rem;color:#718096;margin-top:3px}
    .container{max-width:1300px;margin:40px auto;padding:0 28px}
    .tabs{display:flex;flex-wrap:wrap;gap:6px;background:#fff;border-radius:12px;padding:8px;margin-bottom:24px;box-shadow:0 2px 8px rgba(0,0,0,.06)}
    .tab-btn{padding:9px 18px;border:none;background:transparent;color:#4a5568;border-radius:8px;cursor:pointer;font-size:.88rem;font-weight:500;transition:all .2s}
    .tab-btn:hover{background:#f7fafc}
    .tab-btn.active{background:linear-gradient(135deg,#e53e3e,#c53030);color:#fff;font-weight:700}
    .card{background:#fff;border-radius:14px;padding:24px;box-shadow:0 2px 8px rgba(0,0,0,.06);margin-bottom:20px}
    .section-title{font-size:1.2rem;font-weight:700;color:#2d3748;margin-bottom:16px;display:flex;align-items:center;gap:8px}
    .form-group{margin-bottom:14px}
    .form-group label{display:block;margin-bottom:4px;font-weight:600;color:#4a5568;font-size:.85rem}
    .form-group input,.form-group textarea,.form-group select{width:100%;padding:9px 12px;border:2px solid #e2e8f0;border-radius:8px;font-size:.9rem;transition:border-color .2s;font-family:inherit}
    .form-group input:focus,.form-group textarea:focus,.form-group select:focus{outline:none;border-color:#e53e3e}
    .form-row{display:grid;grid-template-columns:1fr 1fr;gap:14px}
    .form-row-3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:14px}
    .btn{padding:8px 18px;border:none;border-radius:8px;cursor:pointer;font-size:.85rem;font-weight:600;transition:all .2s;display:inline-flex;align-items:center;gap:5px;text-decoration:none}
    .btn-primary{background:linear-gradient(135deg,#e53e3e,#c53030);color:#fff}
    .btn-primary:hover{transform:translateY(-1px);box-shadow:0 3px 10px rgba(229,62,62,.4)}
    .btn-secondary{background:#f7fafc;color:#4a5568;border:2px solid #e2e8f0}
    .btn-secondary:hover{background:#edf2f7}
    .btn-danger{background:#fff5f5;color:#c53030;border:2px solid #fed7d7}
    .btn-danger:hover{background:#fed7d7}
    .btn-success{background:#f0fff4;color:#22543d;border:2px solid #c6f6d5}
    .btn-success:hover{background:#c6f6d5}
    .btn-sm{padding:5px 11px;font-size:.78rem}
    .tbl{width:100%;border-collapse:collapse}
    .tbl th{background:#f7fafc;padding:9px 12px;text-align:left;font-size:.82rem;color:#4a5568;border-bottom:2px solid #e2e8f0;white-space:nowrap}
    .tbl td{padding:9px 12px;border-bottom:1px solid #e2e8f0;font-size:.87rem;color:#2d3748;vertical-align:middle}
    .tbl tr:hover td{background:#fff5f5}
    .badge{padding:3px 9px;border-radius:20px;font-size:.72rem;font-weight:700;white-space:nowrap}
    .badge-published,.badge-active,.badge-true{background:#c6f6d5;color:#22543d}
    .badge-draft,.badge-inactive,.badge-false{background:#fed7d7;color:#c53030}
    .badge-in_progress,.badge-enrolled{background:#bee3f8;color:#2b6cb0}
    .badge-completed{background:#c6f6d5;color:#22543d}
    .badge-paused,.badge-intermediate{background:#fbd38d;color:#c05621}
    .badge-beginner{background:#bee3f8;color:#2b6cb0}
    .badge-advanced{background:#fbd38d;color:#c05621}
    .badge-video{background:#e9d8fd;color:#553c9a}
    .badge-text{background:#bee3f8;color:#2b6cb0}
    .badge-quiz{background:#fbb6ce;color:#97266d}
    .badge-assignment{background:#fbd38d;color:#c05621}
    .alert{padding:10px 14px;border-radius:8px;margin-bottom:14px;font-size:.88rem}
    .alert-success{background:#c6f6d5;color:#22543d}
    .alert-error{background:#fed7d7;color:#c53030}
    .hidden{display:none!important}
    .loading{text-align:center;padding:32px;color:#718096}
    .spinner{border:3px solid #e2e8f0;border-top:3px solid #e53e3e;border-radius:50%;width:28px;height:28px;animation:spin .8s linear infinite;margin:0 auto 10px}
    @keyframes spin{to{transform:rotate(360deg)}}
    .empty-state{text-align:center;padding:40px;color:#a0aec0}
    .empty-state .icon{font-size:2.5rem;margin-bottom:10px}
    .filter-bar{display:flex;gap:10px;flex-wrap:wrap;align-items:flex-end;margin-bottom:16px;background:#f7fafc;border-radius:10px;padding:14px}
    .filter-bar .form-group{margin:0;min-width:160px;flex:1}
    .filter-bar .form-group label{font-size:.78rem}
    .filter-bar .form-group input,.filter-bar .form-group select{padding:7px 10px;font-size:.85rem}
    .pagination{display:flex;gap:8px;align-items:center;margin-top:14px;font-size:.85rem;color:#718096}
    .pagination button{padding:5px 11px;border:1px solid #e2e8f0;border-radius:6px;background:#fff;cursor:pointer;font-size:.82rem}
    .pagination button:hover{background:#f7fafc}
    .pagination button:disabled{opacity:.4;cursor:default}
    .modal-overlay{position:fixed;top:0;left:0;right:0;bottom:0;background:rgba(0,0,0,.5);display:flex;align-items:center;justify-content:center;z-index:1000}
    .modal-box{background:#fff;border-radius:14px;padding:28px;width:560px;max-width:95vw;max-height:90vh;overflow-y:auto;box-shadow:0 20px 60px rgba(0,0,0,.25)}
    .modal-header{display:flex;justify-content:space-between;align-items:center;margin-bottom:20px}
    .modal-header h2{font-size:1.1rem;color:#2d3748}
    .modal-close{background:none;border:none;font-size:1.4rem;cursor:pointer;color:#a0aec0;line-height:1}
    .modal-close:hover{color:#2d3748}
    .modal-footer{display:flex;gap:10px;justify-content:flex-end;margin-top:20px}
    .dm-toggle{background:transparent;border:1px solid rgba(255,255,255,.35);color:#fff;border-radius:20px;padding:5px 14px;cursor:pointer;font-size:.9rem;line-height:1;transition:background .2s;white-space:nowrap}
    .dm-toggle:hover{background:rgba(255,255,255,.15)}
    html[data-theme=dark] body{background:#0f1117}
    html[data-theme=dark] .topbar{background:linear-gradient(135deg,#742a2a,#5a1a1a)}
    html[data-theme=dark] .hero{background:linear-gradient(135deg,#742a2a,#5a1a1a)}
    html[data-theme=dark] .stat-card{background:#1a1d27;box-shadow:0 4px 16px rgba(0,0,0,.4)}
    html[data-theme=dark] .stat-num{color:#fc8181}
    html[data-theme=dark] .stat-label{color:#718096}
    html[data-theme=dark] .tabs{background:#1a1d27;box-shadow:0 2px 8px rgba(0,0,0,.3)}
    html[data-theme=dark] .tab-btn{color:rgba(255,255,255,.7)}
    html[data-theme=dark] .tab-btn:hover{background:#252837}
    html[data-theme=dark] .tab-btn.active{background:linear-gradient(135deg,#742a2a,#5a1a1a)}
    html[data-theme=dark] .card{background:#1a1d27;box-shadow:0 2px 8px rgba(0,0,0,.3)}
    html[data-theme=dark] .section-title{color:#e2e8f0}
    html[data-theme=dark] .form-group label{color:#a0aec0}
    html[data-theme=dark] .form-group input,html[data-theme=dark] .form-group select,html[data-theme=dark] .form-group textarea{background:#252837;color:#e2e8f0;border-color:#4a5568}
    html[data-theme=dark] .form-group input:focus,html[data-theme=dark] .form-group select:focus{border-color:#fc8181}
    html[data-theme=dark] .btn-secondary{background:#252837;color:#e2e8f0;border-color:#4a5568}
    html[data-theme=dark] .btn-danger{background:#3a1a1a;color:#fc8181;border-color:#742a2a}
    html[data-theme=dark] .btn-success{background:#1a3a23;color:#68d391;border-color:#276749}
    html[data-theme=dark] .tbl th{background:#14172a;color:#a0aec0;border-bottom-color:#4a5568}
    html[data-theme=dark] .tbl td{border-bottom-color:#2d3748;color:#e2e8f0}
    html[data-theme=dark] .tbl tr:hover td{background:#1e1a2e}
    html[data-theme=dark] .filter-bar{background:#14172a}
    html[data-theme=dark] .alert-success{background:#1a3a23;color:#68d391}
    html[data-theme=dark] .alert-error{background:#3a1a1a;color:#fc8181}
    html[data-theme=dark] .modal-box{background:#1a1d27}
    html[data-theme=dark] .modal-header h2{color:#e2e8f0}
    html[data-theme=dark] .modal-close{color:#718096}
    html[data-theme=dark] .modal-close:hover{color:#e2e8f0}
    html[data-theme=dark] .pagination button{background:#252837;border-color:#4a5568;color:#e2e8f0}
    html[data-theme=dark] .pagination{color:#a0aec0}
  </style>
</head>
<body>
<div class="topbar">
  <div class="topbar-brand">&#9881; LMS Admin <span class="topbar-badge">ADMIN</span></div>
  <div class="topbar-nav">
    <a href="x_1879300_learning_student_dashboard.do">Student Dashboard</a>
    <a href="x_1879300_learning_instructor_dashboard.do">Instructor Dashboard</a>
    <a href="x_1879300_learning_catalog.do">Catalog</a>
    <button id="dm-toggle" class="dm-toggle" onclick="toggleDark()" title="Toggle dark mode">&#127769;</button>
  </div>
</div>
<div class="hero">
  <h1>&#9881; Admin Dashboard</h1>
  <p>Manage all courses, lessons, enrollments, assessments, and users across the LMS</p>
</div>
<div class="stats-row" id="stats-row">
  <div class="stat-card"><div class="stat-num" id="stat-courses">&#8212;</div><div class="stat-label">Total Courses</div></div>
  <div class="stat-card"><div class="stat-num" id="stat-lessons">&#8212;</div><div class="stat-label">Total Lessons</div></div>
  <div class="stat-card"><div class="stat-num" id="stat-enrollments">&#8212;</div><div class="stat-label">Enrollments</div></div>
  <div class="stat-card"><div class="stat-num" id="stat-assessments">&#8212;</div><div class="stat-label">Assessments</div></div>
  <div class="stat-card"><div class="stat-num" id="stat-completed">&#8212;</div><div class="stat-label">Completions</div></div>
</div>
<div class="container">
  <div id="msg-global"></div>
  <nav class="tabs">
    <button class="tab-btn active" data-tab="courses">&#128218; Courses</button>
    <button class="tab-btn" data-tab="lessons">&#128196; Lessons</button>
    <button class="tab-btn" data-tab="enrollments">&#128100; Enrollments</button>
    <button class="tab-btn" data-tab="assessments">&#10003; Assessments</button>
    <button class="tab-btn" data-tab="users">&#128101; Users</button>
  </nav>

  <!-- TAB: Courses -->
  <div id="tab-courses" class="tab-pane">
    <div class="filter-bar">
      <div class="form-group"><label>Search Title</label><input type="text" id="cf-title" placeholder="Title..." oninput="debounceLoadCourses()" /></div>
      <div class="form-group"><label>Category</label>
        <select id="cf-cat" onchange="loadCourses()">
          <option value="">All</option>
          <option value="programming">Programming</option>
          <option value="web_development">Web Development</option>
          <option value="data_science">Data Science</option>
          <option value="devops">DevOps</option>
          <option value="mobile">Mobile</option>
          <option value="cybersecurity">Cybersecurity</option>
          <option value="cloud">Cloud</option>
          <option value="other">Other</option>
        </select>
      </div>
      <div class="form-group"><label>Status</label>
        <select id="cf-pub" onchange="loadCourses()">
          <option value="">All</option>
          <option value="true">Published</option>
          <option value="false">Draft</option>
        </select>
      </div>
      <div style="display:flex;align-items:flex-end"><button class="btn btn-primary" onclick="openNewCourse()">+ New Course</button></div>
    </div>
    <div class="card" style="padding:0;overflow:hidden">
      <div id="courses-table"><div class="loading"><div class="spinner"></div>Loading...</div></div>
    </div>
    <div class="pagination" id="courses-pager"></div>
  </div>

  <!-- TAB: Lessons -->
  <div id="tab-lessons" class="tab-pane hidden">
    <div class="filter-bar">
      <div class="form-group"><label>Filter by Course</label>
        <select id="lf-course" onchange="loadLessons()"><option value="">All Courses</option></select>
      </div>
      <div class="form-group"><label>Search Title</label><input type="text" id="lf-title" placeholder="Lesson title..." oninput="debounceLoadLessons()" /></div>
      <div class="form-group"><label>Type</label>
        <select id="lf-type" onchange="loadLessons()">
          <option value="">All Types</option>
          <option value="text">Text</option>
          <option value="video">Video</option>
          <option value="quiz">Quiz</option>
          <option value="assignment">Assignment</option>
        </select>
      </div>
    </div>
    <div class="card" style="padding:0;overflow:hidden">
      <div id="lessons-table"><div class="loading"><div class="spinner"></div>Loading...</div></div>
    </div>
    <div class="pagination" id="lessons-pager"></div>
  </div>

  <!-- TAB: Enrollments -->
  <div id="tab-enrollments" class="tab-pane hidden">
    <div class="filter-bar">
      <div class="form-group"><label>Filter by Course</label>
        <select id="ef-course" onchange="loadEnrollments()"><option value="">All Courses</option></select>
      </div>
      <div class="form-group"><label>Status</label>
        <select id="ef-status" onchange="loadEnrollments()">
          <option value="">All</option>
          <option value="enrolled">Enrolled</option>
          <option value="in_progress">In Progress</option>
          <option value="completed">Completed</option>
          <option value="paused">Paused</option>
        </select>
      </div>
      <div class="form-group"><label>Search Student</label><input type="text" id="ef-student" placeholder="Name or username..." oninput="debounceLoadEnrollments()" /></div>
    </div>
    <div class="card" style="padding:0;overflow:hidden">
      <div id="enrollments-table"><div class="loading"><div class="spinner"></div>Loading...</div></div>
    </div>
    <div class="pagination" id="enrollments-pager"></div>
  </div>

  <!-- TAB: Assessments -->
  <div id="tab-assessments" class="tab-pane hidden">
    <div class="filter-bar">
      <div class="form-group"><label>Filter by Course</label>
        <select id="af-course" onchange="loadAssessments()"><option value="">All Courses</option></select>
      </div>
      <div class="form-group"><label>Type</label>
        <select id="af-type" onchange="loadAssessments()">
          <option value="">All Types</option>
          <option value="quiz">Quiz</option>
          <option value="exam">Exam</option>
          <option value="assignment">Assignment</option>
          <option value="survey">Survey</option>
        </select>
      </div>
    </div>
    <div class="card" style="padding:0;overflow:hidden">
      <div id="assessments-table"><div class="loading"><div class="spinner"></div>Loading...</div></div>
    </div>
    <div class="pagination" id="assessments-pager"></div>
  </div>

  <!-- TAB: Users -->
  <div id="tab-users" class="tab-pane hidden">
    <div class="filter-bar">
      <div class="form-group" style="flex:2"><label>Search Users</label><input type="text" id="uf-search" placeholder="Name, username, or email..." /></div>
      <div style="display:flex;align-items:flex-end"><button class="btn btn-primary" onclick="searchUsers()">&#128269; Search</button></div>
    </div>
    <div class="card" style="padding:0;overflow:hidden">
      <div id="users-table"><div class="empty-state"><div class="icon">&#128101;</div><p>Search for a user above.</p></div></div>
    </div>
    <div id="user-enrollments-panel" class="hidden">
      <div class="card">
        <div class="section-title" id="user-enroll-title">Enrollments</div>
        <div id="user-enrollments-table"></div>
      </div>
    </div>
  </div>
</div>

<!-- Modal: Edit/Create Course -->
<div id="modal-course" class="modal-overlay hidden">
  <div class="modal-box">
    <div class="modal-header">
      <h2 id="modal-course-title">Edit Course</h2>
      <button class="modal-close" onclick="closeModal('modal-course')">&times;</button>
    </div>
    <div id="modal-course-msg"></div>
    <form id="form-modal-course">
      <input type="hidden" name="sys_id" />
      <div class="form-row">
        <div class="form-group"><label>Title *</label><input type="text" name="title" required="required" /></div>
        <div class="form-group"><label>Category *</label>
          <select name="category" required="required">
            <option value="">Select...</option>
            <option value="programming">Programming</option>
            <option value="web_development">Web Development</option>
            <option value="data_science">Data Science</option>
            <option value="devops">DevOps</option>
            <option value="mobile">Mobile</option>
            <option value="cybersecurity">Cybersecurity</option>
            <option value="cloud">Cloud</option>
            <option value="other">Other</option>
          </select>
        </div>
      </div>
      <div class="form-group"><label>Description</label><textarea name="description" rows="3"></textarea></div>
      <div class="form-row">
        <div class="form-group"><label>Difficulty</label>
          <select name="difficulty_level">
            <option value="">Select...</option>
            <option value="beginner">Beginner</option>
            <option value="intermediate">Intermediate</option>
            <option value="advanced">Advanced</option>
          </select>
        </div>
        <div class="form-group"><label>Duration (hours)</label><input type="number" name="estimated_duration_hours" min="0" /></div>
      </div>
      <div class="form-row">
        <div class="form-group"><label style="display:flex;align-items:center;gap:6px;cursor:pointer"><input type="checkbox" name="published" style="width:auto" /> Published</label></div>
        <div class="form-group"><label style="display:flex;align-items:center;gap:6px;cursor:pointer"><input type="checkbox" name="active" style="width:auto" /> Active</label></div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" onclick="closeModal('modal-course')">Cancel</button>
        <button type="submit" class="btn btn-primary" id="modal-course-submit">Save</button>
      </div>
    </form>
  </div>
</div>

<!-- Modal: Edit Lesson -->
<div id="modal-lesson" class="modal-overlay hidden">
  <div class="modal-box">
    <div class="modal-header">
      <h2>Edit Lesson</h2>
      <button class="modal-close" onclick="closeModal('modal-lesson')">&times;</button>
    </div>
    <div id="modal-lesson-msg"></div>
    <form id="form-modal-lesson">
      <input type="hidden" name="sys_id" />
      <div class="form-group"><label>Title *</label><input type="text" name="title" required="required" /></div>
      <div class="form-row">
        <div class="form-group"><label>Type</label>
          <select name="lesson_type">
            <option value="text">Text</option>
            <option value="video">Video</option>
            <option value="quiz">Quiz</option>
            <option value="assignment">Assignment</option>
          </select>
        </div>
        <div class="form-group"><label>Order #</label><input type="number" name="order_sequence" min="1" /></div>
      </div>
      <div class="form-group"><label>Description</label><textarea name="description" rows="3"></textarea></div>
      <div class="form-group"><label>Video URL (if video type)</label><input type="text" name="video_url" /></div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" onclick="closeModal('modal-lesson')">Cancel</button>
        <button type="submit" class="btn btn-primary">Save</button>
      </div>
    </form>
  </div>
</div>

<!-- Modal: Edit Enrollment -->
<div id="modal-enrollment" class="modal-overlay hidden">
  <div class="modal-box">
    <div class="modal-header">
      <h2>Edit Enrollment</h2>
      <button class="modal-close" onclick="closeModal('modal-enrollment')">&times;</button>
    </div>
    <div id="modal-enrollment-msg"></div>
    <form id="form-modal-enrollment">
      <input type="hidden" name="sys_id" />
      <div class="form-group"><label>Student</label><input type="text" name="student_display" readonly="readonly" style="background:#f7fafc" /></div>
      <div class="form-group"><label>Course</label><input type="text" name="course_display" readonly="readonly" style="background:#f7fafc" /></div>
      <div class="form-group"><label>Status</label>
        <select name="status">
          <option value="enrolled">Enrolled</option>
          <option value="in_progress">In Progress</option>
          <option value="completed">Completed</option>
          <option value="paused">Paused</option>
        </select>
      </div>
      <div class="form-group"><label>Progress %</label><input type="number" name="progress_percentage" min="0" max="100" /></div>
      <div class="form-group"><label>Completion Date</label><input type="date" name="completion_date" /></div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" onclick="closeModal('modal-enrollment')">Cancel</button>
        <button type="submit" class="btn btn-primary">Save</button>
      </div>
    </form>
  </div>
</div>

<!-- Modal: Edit Assessment -->
<div id="modal-assessment" class="modal-overlay hidden">
  <div class="modal-box">
    <div class="modal-header">
      <h2>Edit Assessment</h2>
      <button class="modal-close" onclick="closeModal('modal-assessment')">&times;</button>
    </div>
    <div id="modal-assessment-msg"></div>
    <form id="form-modal-assessment">
      <input type="hidden" name="sys_id" />
      <div class="form-group"><label>Title *</label><input type="text" name="title" required="required" /></div>
      <div class="form-row">
        <div class="form-group"><label>Type</label>
          <select name="assessment_type">
            <option value="quiz">Quiz</option>
            <option value="exam">Exam</option>
            <option value="assignment">Assignment</option>
            <option value="survey">Survey</option>
          </select>
        </div>
        <div class="form-group"><label>Passing Score %</label><input type="number" name="passing_score" min="0" max="100" /></div>
      </div>
      <div class="form-row">
        <div class="form-group"><label>Max Attempts</label><input type="number" name="max_attempts" min="1" /></div>
        <div class="form-group"><label>Time Limit (min)</label><input type="number" name="time_limit_minutes" min="0" /></div>
      </div>
      <div class="form-group"><label style="display:flex;align-items:center;gap:6px;cursor:pointer"><input type="checkbox" name="active" style="width:auto" /> Active</label></div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" onclick="closeModal('modal-assessment')">Cancel</button>
        <button type="submit" class="btn btn-primary">Save</button>
      </div>
    </form>
  </div>
</div>

<script>
var API = '/api/now/table';
var SCOPE = 'x_1879300_learning_';
var currentUser = window.SN_USER_ID || '';
var allCourses = [];
var courseTimer = null, lessonTimer = null, enrollTimer = null;

function initDark(){var d=document.documentElement.getAttribute('data-theme')==='dark';var b=document.getElementById('dm-toggle');if(b)b.textContent=d?'\u2600\uFE0F':'\uD83C\uDF19';}
function toggleDark(){var d=document.documentElement.getAttribute('data-theme')==='dark';document.documentElement.setAttribute('data-theme',d?'light':'dark');localStorage.setItem('lms-theme',d?'light':'dark');var b=document.getElementById('dm-toggle');if(b)b.textContent=d?'\uD83C\uDF19':'\u2600\uFE0F';}
initDark();

function hdrs(){var ck=window.SN_TOKEN||window.g_ck||(typeof g_ck!=='undefined'?g_ck:'');return{'Accept':'application/json','Content-Type':'application/json','X-UserToken':ck};}
function showMsg(msg,type){var el=document.getElementById('msg-global');el.innerHTML='<div class="alert alert-'+type+'">'+msg+'</div>';if(type!=='error')setTimeout(function(){el.innerHTML='';},5000);}
function getVal(obj,field){if(!obj)return'';var v=obj[field];if(v&&typeof v==='object')return v.display_value||v.value||'';return v||'';}
function getRawVal(obj,field){if(!obj)return'';var v=obj[field];if(v&&typeof v==='object')return v.value||'';return v||'';}
function esc(s){return(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}
function escQ(s){return(s||'').replace(/'/g,"\\'");}
function closeModal(id){document.getElementById(id).classList.add('hidden');}
function debounce(fn,delay){return function(){clearTimeout(arguments.callee._t);arguments.callee._t=setTimeout(fn,delay);};}
function debounceLoadCourses(){clearTimeout(courseTimer);courseTimer=setTimeout(loadCourses,350);}
function debounceLoadLessons(){clearTimeout(lessonTimer);lessonTimer=setTimeout(loadLessons,350);}
function debounceLoadEnrollments(){clearTimeout(enrollTimer);enrollTimer=setTimeout(loadEnrollments,350);}

// Tab switching
document.querySelectorAll('.tab-btn').forEach(function(btn){
  btn.addEventListener('click',function(){
    document.querySelectorAll('.tab-btn').forEach(function(b){b.classList.remove('active');});
    document.querySelectorAll('.tab-pane').forEach(function(p){p.classList.add('hidden');});
    btn.classList.add('active');
    var tab=btn.dataset.tab;
    document.getElementById('tab-'+tab).classList.remove('hidden');
    if(tab==='courses')loadCourses();
    if(tab==='lessons'){populateCourseFilter('lf-course');loadLessons();}
    if(tab==='enrollments'){populateCourseFilter('ef-course');loadEnrollments();}
    if(tab==='assessments'){populateCourseFilter('af-course');loadAssessments();}
  });
});

// ─── Overview Stats ───────────────────────────────────────────────
function loadStats(){
  var tables=['courses','lessons','student_enrollments','assessments'];
  var ids=['stat-courses','stat-lessons','stat-enrollments','stat-assessments'];
  tables.forEach(function(t,i){
    fetch(API+'/'+SCOPE+t+'?sysparm_limit=1&sysparm_fields=sys_id',{headers:hdrs()})
      .then(function(r){return r.headers.get('X-Total-Count')||r.json().then(function(d){return(d.result||[]).length;});})
      .then(function(n){document.getElementById(ids[i]).textContent=n;})
      .catch(function(){document.getElementById(ids[i]).textContent='?';});
  });
  fetch(API+'/'+SCOPE+'student_enrollments?sysparm_query=status=completed&sysparm_limit=1&sysparm_fields=sys_id',{headers:hdrs()})
    .then(function(r){return r.headers.get('X-Total-Count')||r.json().then(function(d){return(d.result||[]).length;});})
    .then(function(n){document.getElementById('stat-completed').textContent=n;})
    .catch(function(){document.getElementById('stat-completed').textContent='?';});
}
loadStats();

// ─── Course filter dropdown (shared) ─────────────────────────────
function populateCourseFilter(selectId){
  var sel=document.getElementById(selectId);
  while(sel.options.length>1)sel.remove(1);
  if(allCourses.length){
    allCourses.forEach(function(c){sel.add(new Option(getVal(c,'title'),getVal(c,'sys_id')));});
    return;
  }
  fetch(API+'/'+SCOPE+'courses?sysparm_fields=sys_id,title&sysparm_limit=500&sysparm_query=ORDERBYtitle',{headers:hdrs()})
    .then(function(r){return r.json();})
    .then(function(d){
      allCourses=d.result||[];
      allCourses.forEach(function(c){sel.add(new Option(getVal(c,'title'),getVal(c,'sys_id')));});
    });
}

// ─── Courses Tab ──────────────────────────────────────────────────
var coursePage=1,coursePageSize=20,courseTotal=0;
function loadCourses(page){
  coursePage=page||1;
  var title=document.getElementById('cf-title').value.trim();
  var cat=document.getElementById('cf-cat').value;
  var pub=document.getElementById('cf-pub').value;
  var q='ORDERBYtitle';
  if(title)q='titleLIKE'+title+'^'+q;
  if(cat)q='category='+cat+'^'+q;
  if(pub!=='')q='published='+pub+'^'+q;
  var offset=(coursePage-1)*coursePageSize;
  var el=document.getElementById('courses-table');
  el.innerHTML='<div class="loading"><div class="spinner"></div>Loading...</div>';
  fetch(API+'/'+SCOPE+'courses?sysparm_query='+encodeURIComponent(q)+'&sysparm_display_value=all&sysparm_limit='+coursePageSize+'&sysparm_offset='+offset,{headers:hdrs()})
    .then(function(r){courseTotal=parseInt(r.headers.get('X-Total-Count')||'0');return r.json();})
    .then(function(data){
      var rows=data.result||[];
      if(!rows.length){el.innerHTML='<div class="empty-state"><div class="icon">&#128218;</div><p>No courses found.</p></div>';renderPager('courses-pager',0,0,0,loadCourses);return;}
      var html='<table class="tbl"><thead><tr><th>Title</th><th>Category</th><th>Difficulty</th><th>Duration</th><th>Status</th><th>Active</th><th>Instructor</th><th></th></tr></thead><tbody>';
      rows.forEach(function(c){
        var cid=getVal(c,'sys_id');
        var pub=c.published===true||c.published==='true';
        var act=c.active===true||c.active==='true';
        var diff=getRawVal(c,'difficulty_level')||getVal(c,'difficulty_level');
        html+='<tr>'+
          '<td><strong>'+esc(getVal(c,'title'))+'</strong></td>'+
          '<td>'+esc(getVal(c,'category'))+'</td>'+
          '<td><span class="badge badge-'+diff+'">'+esc(diff)+'</span></td>'+
          '<td>'+(getVal(c,'estimated_duration_hours')||'—')+'h</td>'+
          '<td><span class="badge badge-'+(pub?'published':'draft')+'">'+(pub?'Published':'Draft')+'</span></td>'+
          '<td><span class="badge badge-'+(act?'true':'false')+'">'+(act?'Yes':'No')+'</span></td>'+
          '<td>'+esc(getVal(c,'created_by'))+'</td>'+
          '<td style="white-space:nowrap">'+
            '<button class="btn btn-secondary btn-sm" style="margin-right:4px" onclick="openEditCourse(\''+cid+'\')">Edit</button>'+
            '<a class="btn btn-secondary btn-sm" style="margin-right:4px;text-decoration:none" href="x_1879300_learning_lms.do?course='+cid+'" target="_blank">&#128065;</a>'+
            '<button class="btn btn-'+(pub?'secondary':'success')+' btn-sm" style="margin-right:4px" onclick="toggleCoursePub(\''+cid+'\','+!pub+')">'+(pub?'Unpublish':'Publish')+'</button>'+
            '<button class="btn btn-danger btn-sm" onclick="deleteCourse(\''+cid+'\',\''+escQ(getVal(c,'title'))+'\')">Del</button>'+
          '</td></tr>';
      });
      html+='</tbody></table>';
      el.innerHTML=html;
      renderPager('courses-pager',coursePage,coursePageSize,courseTotal,loadCourses);
      allCourses=[];// refresh cache on next filter use
    })
    .catch(function(){el.innerHTML='<div class="alert alert-error" style="margin:16px">Failed to load courses.</div>';});
}

function toggleCoursePub(id,newPub){
  fetch(API+'/'+SCOPE+'courses/'+id,{method:'PATCH',headers:hdrs(),body:JSON.stringify({published:newPub})})
    .then(function(){loadCourses(coursePage);showMsg('Course '+(newPub?'published':'unpublished')+'!','success');});
}

function deleteCourse(id,title){
  if(!confirm('Delete course "'+title+'"? This cannot be undone.'))return;
  fetch(API+'/'+SCOPE+'courses/'+id,{method:'DELETE',headers:hdrs()})
    .then(function(r){if(!r.ok)throw new Error();showMsg('Course deleted.','success');loadCourses(1);})
    .catch(function(){showMsg('Failed to delete course.','error');});
}

function openNewCourse(){
  document.getElementById('modal-course-title').textContent='Create Course';
  document.getElementById('modal-course-submit').textContent='Create';
  var form=document.getElementById('form-modal-course');
  form.reset();
  form.querySelector('[name=sys_id]').value='';
  document.getElementById('modal-course-msg').innerHTML='';
  document.getElementById('modal-course').classList.remove('hidden');
}

function openEditCourse(id){
  fetch(API+'/'+SCOPE+'courses/'+id+'?sysparm_display_value=all',{headers:hdrs()})
    .then(function(r){return r.json();})
    .then(function(d){
      var c=d.result;
      if(!c)return;
      document.getElementById('modal-course-title').textContent='Edit Course';
      document.getElementById('modal-course-submit').textContent='Save Changes';
      var form=document.getElementById('form-modal-course');
      form.querySelector('[name=sys_id]').value=getRawVal(c,'sys_id')||getVal(c,'sys_id');
      form.querySelector('[name=title]').value=getVal(c,'title');
      form.querySelector('[name=category]').value=getRawVal(c,'category');
      form.querySelector('[name=description]').value=getVal(c,'description');
      form.querySelector('[name=difficulty_level]').value=getRawVal(c,'difficulty_level');
      form.querySelector('[name=estimated_duration_hours]').value=getRawVal(c,'estimated_duration_hours')||getVal(c,'estimated_duration_hours');
      form.querySelector('[name=published]').checked=c.published===true||c.published==='true';
      form.querySelector('[name=active]').checked=c.active===true||c.active==='true';
      document.getElementById('modal-course-msg').innerHTML='';
      document.getElementById('modal-course').classList.remove('hidden');
    });
}

document.getElementById('form-modal-course').addEventListener('submit',function(e){
  e.preventDefault();
  var fd=new FormData(e.target);
  var id=fd.get('sys_id');
  var body={title:fd.get('title'),description:fd.get('description'),category:fd.get('category'),difficulty_level:fd.get('difficulty_level'),estimated_duration_hours:fd.get('estimated_duration_hours')||0,published:fd.has('published'),active:fd.has('active')};
  var url=id?API+'/'+SCOPE+'courses/'+id:API+'/'+SCOPE+'courses';
  var method=id?'PATCH':'POST';
  if(!id)body.created_by=currentUser;
  fetch(url,{method:method,headers:hdrs(),body:JSON.stringify(body)})
    .then(function(r){if(!r.ok)throw new Error();closeModal('modal-course');showMsg('Course saved!','success');loadCourses(coursePage);loadStats();})
    .catch(function(){document.getElementById('modal-course-msg').innerHTML='<div class="alert alert-error">Failed to save course.</div>';});
});

// ─── Lessons Tab ──────────────────────────────────────────────────
var lessonPage=1,lessonPageSize=25,lessonTotal=0;
function loadLessons(page){
  lessonPage=page||1;
  var course=document.getElementById('lf-course').value;
  var title=document.getElementById('lf-title').value.trim();
  var type=document.getElementById('lf-type').value;
  var q='ORDERBYcourse^ORDERBYorder_sequence';
  if(course)q='course='+course+'^'+q;
  if(title)q='titleLIKE'+title+'^'+q;
  if(type)q='lesson_type='+type+'^'+q;
  var offset=(lessonPage-1)*lessonPageSize;
  var el=document.getElementById('lessons-table');
  el.innerHTML='<div class="loading"><div class="spinner"></div>Loading...</div>';
  fetch(API+'/'+SCOPE+'lessons?sysparm_query='+encodeURIComponent(q)+'&sysparm_display_value=all&sysparm_limit='+lessonPageSize+'&sysparm_offset='+offset,{headers:hdrs()})
    .then(function(r){lessonTotal=parseInt(r.headers.get('X-Total-Count')||'0');return r.json();})
    .then(function(data){
      var rows=data.result||[];
      if(!rows.length){el.innerHTML='<div class="empty-state"><div class="icon">&#128196;</div><p>No lessons found.</p></div>';renderPager('lessons-pager',0,0,0,loadLessons);return;}
      var html='<table class="tbl"><thead><tr><th>#</th><th>Title</th><th>Course</th><th>Type</th><th></th></tr></thead><tbody>';
      rows.forEach(function(l){
        var lid=getVal(l,'sys_id');
        var t=getRawVal(l,'lesson_type')||getVal(l,'lesson_type');
        html+='<tr>'+
          '<td>'+esc(getVal(l,'order_sequence'))+'</td>'+
          '<td>'+esc(getVal(l,'title'))+'</td>'+
          '<td>'+esc(getVal(l,'course'))+'</td>'+
          '<td><span class="badge badge-'+t+'">'+esc(t)+'</span></td>'+
          '<td style="white-space:nowrap">'+
            '<button class="btn btn-secondary btn-sm" style="margin-right:4px" onclick="openEditLesson(\''+lid+'\')">Edit</button>'+
            '<button class="btn btn-danger btn-sm" onclick="deleteLesson(\''+lid+'\',\''+escQ(getVal(l,'title'))+'\')">Del</button>'+
          '</td></tr>';
      });
      html+='</tbody></table>';
      el.innerHTML=html;
      renderPager('lessons-pager',lessonPage,lessonPageSize,lessonTotal,loadLessons);
    })
    .catch(function(){el.innerHTML='<div class="alert alert-error" style="margin:16px">Failed to load lessons.</div>';});
}

function openEditLesson(id){
  fetch(API+'/'+SCOPE+'lessons/'+id+'?sysparm_display_value=all',{headers:hdrs()})
    .then(function(r){return r.json();})
    .then(function(d){
      var l=d.result;if(!l)return;
      var form=document.getElementById('form-modal-lesson');
      form.querySelector('[name=sys_id]').value=getRawVal(l,'sys_id')||getVal(l,'sys_id');
      form.querySelector('[name=title]').value=getVal(l,'title');
      form.querySelector('[name=lesson_type]').value=getRawVal(l,'lesson_type')||getVal(l,'lesson_type');
      form.querySelector('[name=order_sequence]').value=getRawVal(l,'order_sequence')||getVal(l,'order_sequence');
      form.querySelector('[name=description]').value=getVal(l,'description');
      form.querySelector('[name=video_url]').value=getRawVal(l,'video_url')||getVal(l,'video_url');
      document.getElementById('modal-lesson-msg').innerHTML='';
      document.getElementById('modal-lesson').classList.remove('hidden');
    });
}

document.getElementById('form-modal-lesson').addEventListener('submit',function(e){
  e.preventDefault();
  var fd=new FormData(e.target);
  var id=fd.get('sys_id');
  var body={title:fd.get('title'),lesson_type:fd.get('lesson_type'),order_sequence:parseInt(fd.get('order_sequence'))||1,description:fd.get('description')||'',video_url:fd.get('video_url')||''};
  fetch(API+'/'+SCOPE+'lessons/'+id,{method:'PATCH',headers:hdrs(),body:JSON.stringify(body)})
    .then(function(r){if(!r.ok)throw new Error();closeModal('modal-lesson');showMsg('Lesson saved!','success');loadLessons(lessonPage);})
    .catch(function(){document.getElementById('modal-lesson-msg').innerHTML='<div class="alert alert-error">Failed to save lesson.</div>';});
});

function deleteLesson(id,title){
  if(!confirm('Delete lesson "'+title+'"?'))return;
  fetch(API+'/'+SCOPE+'lessons/'+id,{method:'DELETE',headers:hdrs()})
    .then(function(r){if(!r.ok)throw new Error();showMsg('Lesson deleted.','success');loadLessons(lessonPage);})
    .catch(function(){showMsg('Failed to delete lesson.','error');});
}

// ─── Enrollments Tab ─────────────────────────────────────────────
var enrollPage=1,enrollPageSize=25,enrollTotal=0;
function loadEnrollments(page){
  enrollPage=page||1;
  var course=document.getElementById('ef-course').value;
  var status=document.getElementById('ef-status').value;
  var student=document.getElementById('ef-student').value.trim();
  var q='ORDERBYDESCsys_created_on';
  if(course)q='course='+course+'^'+q;
  if(status)q='status='+status+'^'+q;
  if(student)q='student.nameLIKE'+student+'^ORstudent.user_nameLIKE'+student+'^'+q;
  var offset=(enrollPage-1)*enrollPageSize;
  var el=document.getElementById('enrollments-table');
  el.innerHTML='<div class="loading"><div class="spinner"></div>Loading...</div>';
  fetch(API+'/'+SCOPE+'student_enrollments?sysparm_query='+encodeURIComponent(q)+'&sysparm_display_value=all&sysparm_limit='+enrollPageSize+'&sysparm_offset='+offset,{headers:hdrs()})
    .then(function(r){enrollTotal=parseInt(r.headers.get('X-Total-Count')||'0');return r.json();})
    .then(function(data){
      var rows=data.result||[];
      if(!rows.length){el.innerHTML='<div class="empty-state"><div class="icon">&#128100;</div><p>No enrollments found.</p></div>';renderPager('enrollments-pager',0,0,0,loadEnrollments);return;}
      var html='<table class="tbl"><thead><tr><th>Student</th><th>Course</th><th>Status</th><th>Progress</th><th>Enrolled</th><th>Completed</th><th></th></tr></thead><tbody>';
      rows.forEach(function(e){
        var eid=getVal(e,'sys_id');
        var st=getRawVal(e,'status')||getVal(e,'status');
        var prog=getRawVal(e,'progress_percentage')||getVal(e,'progress_percentage')||0;
        html+='<tr>'+
          '<td>'+esc(getVal(e,'student'))+'</td>'+
          '<td>'+esc(getVal(e,'course'))+'</td>'+
          '<td><span class="badge badge-'+st+'">'+esc(st)+'</span></td>'+
          '<td>'+prog+'%</td>'+
          '<td>'+(getVal(e,'enrollment_date')||'—').split(' ')[0]+'</td>'+
          '<td>'+(getVal(e,'completion_date')||'—').split(' ')[0]+'</td>'+
          '<td style="white-space:nowrap">'+
            '<button class="btn btn-secondary btn-sm" style="margin-right:4px" onclick="openEditEnrollment(\''+eid+'\')">Edit</button>'+
            '<button class="btn btn-danger btn-sm" onclick="deleteEnrollment(\''+eid+'\')">Del</button>'+
          '</td></tr>';
      });
      html+='</tbody></table>';
      el.innerHTML=html;
      renderPager('enrollments-pager',enrollPage,enrollPageSize,enrollTotal,loadEnrollments);
    })
    .catch(function(){el.innerHTML='<div class="alert alert-error" style="margin:16px">Failed to load enrollments.</div>';});
}

function openEditEnrollment(id){
  fetch(API+'/'+SCOPE+'student_enrollments/'+id+'?sysparm_display_value=all',{headers:hdrs()})
    .then(function(r){return r.json();})
    .then(function(d){
      var e=d.result;if(!e)return;
      var form=document.getElementById('form-modal-enrollment');
      form.querySelector('[name=sys_id]').value=getRawVal(e,'sys_id')||getVal(e,'sys_id');
      form.querySelector('[name=student_display]').value=getVal(e,'student');
      form.querySelector('[name=course_display]').value=getVal(e,'course');
      form.querySelector('[name=status]').value=getRawVal(e,'status')||getVal(e,'status');
      form.querySelector('[name=progress_percentage]').value=getRawVal(e,'progress_percentage')||getVal(e,'progress_percentage')||0;
      var cd=getRawVal(e,'completion_date')||getVal(e,'completion_date')||'';
      form.querySelector('[name=completion_date]').value=cd.split(' ')[0]||'';
      document.getElementById('modal-enrollment-msg').innerHTML='';
      document.getElementById('modal-enrollment').classList.remove('hidden');
    });
}

document.getElementById('form-modal-enrollment').addEventListener('submit',function(e){
  e.preventDefault();
  var fd=new FormData(e.target);
  var id=fd.get('sys_id');
  var body={status:fd.get('status'),progress_percentage:parseInt(fd.get('progress_percentage'))||0};
  if(fd.get('completion_date'))body.completion_date=fd.get('completion_date');
  fetch(API+'/'+SCOPE+'student_enrollments/'+id,{method:'PATCH',headers:hdrs(),body:JSON.stringify(body)})
    .then(function(r){if(!r.ok)throw new Error();closeModal('modal-enrollment');showMsg('Enrollment updated!','success');loadEnrollments(enrollPage);loadStats();})
    .catch(function(){document.getElementById('modal-enrollment-msg').innerHTML='<div class="alert alert-error">Failed to update enrollment.</div>';});
});

function deleteEnrollment(id){
  if(!confirm('Delete this enrollment record? This cannot be undone.'))return;
  fetch(API+'/'+SCOPE+'student_enrollments/'+id,{method:'DELETE',headers:hdrs()})
    .then(function(r){if(!r.ok)throw new Error();showMsg('Enrollment deleted.','success');loadEnrollments(enrollPage);loadStats();})
    .catch(function(){showMsg('Failed to delete enrollment.','error');});
}

// ─── Assessments Tab ─────────────────────────────────────────────
var assessPage=1,assessPageSize=25,assessTotal=0;
function loadAssessments(page){
  assessPage=page||1;
  var course=document.getElementById('af-course').value;
  var type=document.getElementById('af-type').value;
  var q='ORDERBYtitle';
  if(course)q='course='+course+'^'+q;
  if(type)q='assessment_type='+type+'^'+q;
  var offset=(assessPage-1)*assessPageSize;
  var el=document.getElementById('assessments-table');
  el.innerHTML='<div class="loading"><div class="spinner"></div>Loading...</div>';
  fetch(API+'/'+SCOPE+'assessments?sysparm_query='+encodeURIComponent(q)+'&sysparm_display_value=all&sysparm_limit='+assessPageSize+'&sysparm_offset='+offset,{headers:hdrs()})
    .then(function(r){assessTotal=parseInt(r.headers.get('X-Total-Count')||'0');return r.json();})
    .then(function(data){
      var rows=data.result||[];
      if(!rows.length){el.innerHTML='<div class="empty-state"><div class="icon">&#10003;</div><p>No assessments found.</p></div>';renderPager('assessments-pager',0,0,0,loadAssessments);return;}
      var html='<table class="tbl"><thead><tr><th>Title</th><th>Course</th><th>Type</th><th>Passing %</th><th>Max Attempts</th><th>Time Limit</th><th>Active</th><th></th></tr></thead><tbody>';
      rows.forEach(function(a){
        var aid=getVal(a,'sys_id');
        var at=getRawVal(a,'assessment_type')||getVal(a,'assessment_type');
        var act=a.active===true||a.active==='true';
        html+='<tr>'+
          '<td><strong>'+esc(getVal(a,'title'))+'</strong></td>'+
          '<td>'+esc(getVal(a,'course'))+'</td>'+
          '<td><span class="badge badge-'+at+'">'+esc(at)+'</span></td>'+
          '<td>'+(getVal(a,'passing_score')||'—')+'%</td>'+
          '<td>'+(getVal(a,'max_attempts')||'—')+'</td>'+
          '<td>'+(getVal(a,'time_limit_minutes')||'—')+(getVal(a,'time_limit_minutes')?'m':'')+'</td>'+
          '<td><span class="badge badge-'+(act?'true':'false')+'">'+(act?'Yes':'No')+'</span></td>'+
          '<td style="white-space:nowrap">'+
            '<button class="btn btn-secondary btn-sm" style="margin-right:4px" onclick="openEditAssessment(\''+aid+'\')">Edit</button>'+
            '<button class="btn btn-danger btn-sm" onclick="deleteAssessment(\''+aid+'\',\''+escQ(getVal(a,'title'))+'\')">Del</button>'+
          '</td></tr>';
      });
      html+='</tbody></table>';
      el.innerHTML=html;
      renderPager('assessments-pager',assessPage,assessPageSize,assessTotal,loadAssessments);
    })
    .catch(function(){el.innerHTML='<div class="alert alert-error" style="margin:16px">Failed to load assessments.</div>';});
}

function openEditAssessment(id){
  fetch(API+'/'+SCOPE+'assessments/'+id+'?sysparm_display_value=all',{headers:hdrs()})
    .then(function(r){return r.json();})
    .then(function(d){
      var a=d.result;if(!a)return;
      var form=document.getElementById('form-modal-assessment');
      form.querySelector('[name=sys_id]').value=getRawVal(a,'sys_id')||getVal(a,'sys_id');
      form.querySelector('[name=title]').value=getVal(a,'title');
      form.querySelector('[name=assessment_type]').value=getRawVal(a,'assessment_type')||getVal(a,'assessment_type');
      form.querySelector('[name=passing_score]').value=getRawVal(a,'passing_score')||getVal(a,'passing_score')||'';
      form.querySelector('[name=max_attempts]').value=getRawVal(a,'max_attempts')||getVal(a,'max_attempts')||'';
      form.querySelector('[name=time_limit_minutes]').value=getRawVal(a,'time_limit_minutes')||getVal(a,'time_limit_minutes')||'';
      form.querySelector('[name=active]').checked=a.active===true||a.active==='true';
      document.getElementById('modal-assessment-msg').innerHTML='';
      document.getElementById('modal-assessment').classList.remove('hidden');
    });
}

document.getElementById('form-modal-assessment').addEventListener('submit',function(e){
  e.preventDefault();
  var fd=new FormData(e.target);
  var id=fd.get('sys_id');
  var body={title:fd.get('title'),assessment_type:fd.get('assessment_type'),active:fd.has('active')};
  if(fd.get('passing_score'))body.passing_score=parseFloat(fd.get('passing_score'));
  if(fd.get('max_attempts'))body.max_attempts=parseInt(fd.get('max_attempts'));
  if(fd.get('time_limit_minutes'))body.time_limit_minutes=parseInt(fd.get('time_limit_minutes'));
  fetch(API+'/'+SCOPE+'assessments/'+id,{method:'PATCH',headers:hdrs(),body:JSON.stringify(body)})
    .then(function(r){if(!r.ok)throw new Error();closeModal('modal-assessment');showMsg('Assessment saved!','success');loadAssessments(assessPage);})
    .catch(function(){document.getElementById('modal-assessment-msg').innerHTML='<div class="alert alert-error">Failed to save assessment.</div>';});
});

function deleteAssessment(id,title){
  if(!confirm('Delete assessment "'+title+'"?'))return;
  fetch(API+'/'+SCOPE+'assessments/'+id,{method:'DELETE',headers:hdrs()})
    .then(function(r){if(!r.ok)throw new Error();showMsg('Assessment deleted.','success');loadAssessments(assessPage);loadStats();})
    .catch(function(){showMsg('Failed to delete assessment.','error');});
}

// ─── Users Tab ───────────────────────────────────────────────────
function searchUsers(){
  var q=document.getElementById('uf-search').value.trim();
  if(!q){showMsg('Enter a search term.','error');return;}
  var el=document.getElementById('users-table');
  el.innerHTML='<div class="loading"><div class="spinner"></div>Searching...</div>';
  document.getElementById('user-enrollments-panel').classList.add('hidden');
  var query='nameLIKE'+q+'^ORuser_nameLIKE'+q+'^ORemailLIKE'+q+'^ORDERBYname';
  fetch(API+'/sys_user?sysparm_query='+encodeURIComponent(query)+'&sysparm_fields=sys_id,name,user_name,email,active&sysparm_limit=30',{headers:hdrs()})
    .then(function(r){return r.json();})
    .then(function(data){
      var rows=data.result||[];
      if(!rows.length){el.innerHTML='<div class="empty-state"><div class="icon">&#128101;</div><p>No users found.</p></div>';return;}
      var html='<table class="tbl"><thead><tr><th>Name</th><th>Username</th><th>Email</th><th>Active</th><th></th></tr></thead><tbody>';
      rows.forEach(function(u){
        var uid=u.sys_id;
        var act=u.active===true||u.active==='true';
        html+='<tr>'+
          '<td><strong>'+esc(u.name||'')+'</strong></td>'+
          '<td>'+esc(u.user_name||'')+'</td>'+
          '<td>'+esc(u.email||'')+'</td>'+
          '<td><span class="badge badge-'+(act?'true':'false')+'">'+(act?'Active':'Inactive')+'</span></td>'+
          '<td><button class="btn btn-secondary btn-sm" onclick="viewUserEnrollments(\''+uid+'\',\''+escQ(u.name||u.user_name)+'\')">&#128218; Enrollments</button></td>'+
          '</tr>';
      });
      html+='</tbody></table>';
      el.innerHTML=html;
    })
    .catch(function(){el.innerHTML='<div class="alert alert-error" style="margin:16px">Failed to search users.</div>';});
}

document.getElementById('uf-search').addEventListener('keydown',function(e){if(e.key==='Enter')searchUsers();});

function viewUserEnrollments(uid,name){
  var panel=document.getElementById('user-enrollments-panel');
  var el=document.getElementById('user-enrollments-table');
  document.getElementById('user-enroll-title').textContent='Enrollments for '+name;
  el.innerHTML='<div class="loading"><div class="spinner"></div>Loading...</div>';
  panel.classList.remove('hidden');
  panel.scrollIntoView({behavior:'smooth',block:'start'});
  fetch(API+'/'+SCOPE+'student_enrollments?sysparm_query=student='+uid+'&sysparm_display_value=all&sysparm_limit=50',{headers:hdrs()})
    .then(function(r){return r.json();})
    .then(function(data){
      var rows=data.result||[];
      if(!rows.length){el.innerHTML='<div class="empty-state"><div class="icon">&#128218;</div><p>No enrollments for this user.</p></div>';return;}
      var html='<table class="tbl"><thead><tr><th>Course</th><th>Status</th><th>Progress</th><th>Enrolled</th><th>Completed</th></tr></thead><tbody>';
      rows.forEach(function(e){
        var st=getRawVal(e,'status')||getVal(e,'status');
        html+='<tr>'+
          '<td>'+esc(getVal(e,'course'))+'</td>'+
          '<td><span class="badge badge-'+st+'">'+esc(st)+'</span></td>'+
          '<td>'+(getRawVal(e,'progress_percentage')||getVal(e,'progress_percentage')||0)+'%</td>'+
          '<td>'+(getVal(e,'enrollment_date')||'—').split(' ')[0]+'</td>'+
          '<td>'+(getVal(e,'completion_date')||'—').split(' ')[0]+'</td>'+
          '</tr>';
      });
      html+='</tbody></table>';
      el.innerHTML=html;
    })
    .catch(function(){el.innerHTML='<div class="alert alert-error">Failed to load enrollments.</div>';});
}

// ─── Pagination ──────────────────────────────────────────────────
function renderPager(containerId,page,pageSize,total,loadFn){
  var el=document.getElementById(containerId);
  if(!el||total<=pageSize){el.innerHTML='';return;}
  var pages=Math.ceil(total/pageSize);
  var html='<button onclick="('+loadFn.name+')('+(page-1)+')" '+(page<=1?'disabled':'')+'>&#8592; Prev</button>';
  html+='<span>Page '+page+' of '+pages+' ('+total+' total)</span>';
  html+='<button onclick="('+loadFn.name+')('+(page+1)+')" '+(page>=pages?'disabled':'')+'>Next &#8594;</button>';
  el.innerHTML=html;
}

// Initial load
loadCourses();
</script>
</body>
</html>]]></html>
        <name>admin_dashboard</name>
        <processing_script/>
        <sys_class_name>sys_ui_page</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2026-05-07 00:00:00</sys_created_on>
        <sys_id>a1b2c3d4e5f60912abcdef1234567890</sys_id>
        <sys_mod_count>0</sys_mod_count>
        <sys_name>admin_dashboard</sys_name>
        <sys_package display_value="Learning Man" source="x_1879300_learning">95470fa283a5f21071a1ba96feaad3cb</sys_package>
        <sys_policy/>
        <sys_scope display_value="Learning Man">95470fa283a5f21071a1ba96feaad3cb</sys_scope>
        <sys_update_name>sys_ui_page_a1b2c3d4e5f60912abcdef1234567890</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2026-05-07 00:00:00</sys_updated_on>
    </sys_ui_page>
</record_update>
